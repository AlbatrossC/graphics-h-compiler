const COMPILER_ASSET_SOURCES=Object.freeze({libs:Object.freeze({jsdos:Object.freeze({urls:['https://js-dos.com/6.22/current/js-dos.js'],localPath:'/libs/js-dos.js'}),wdosbox:Object.freeze({urls:['https://js-dos.com/6.22/current/wdosbox.js'],localPath:'/libs/wdosbox.js'})}),assets:Object.freeze({'tc-zip':Object.freeze({urls:['https://r2-public-assets.albatrossc.workers.dev/system/tc-v1.zip','https://ltjlklxc9homgiye.public.blob.vercel-storage.com/zips/tc-v1.zip'],localPath:'/compiler-assets/zip-files/tc-v1.zip'}),'demo-files-v1':Object.freeze({urls:[],localPath:'/compiler-assets/Demo_files/demo-files-v1.json'})}),demos:Object.freeze({'graphics-demo':Object.freeze({urls:['https://r2-public-assets.albatrossc.workers.dev/demo/graphics_demo.cpp','https://ltjlklxc9homgiye.public.blob.vercel-storage.com/demo/graphics_demo.cpp'],localPath:'/compiler-assets/Demo_files/graphics_demo.cpp'}),'circle-pattern':Object.freeze({urls:['https://r2-public-assets.albatrossc.workers.dev/demo/circle_pattern.cpp','https://ltjlklxc9homgiye.public.blob.vercel-storage.com/demo/circle-pattern.cpp'],localPath:'/compiler-assets/Demo_files/circle_pattern.cpp'}),'bouncing-ball':Object.freeze({urls:['https://r2-public-assets.albatrossc.workers.dev/demo/bouncing_ball.cpp','https://ltjlklxc9homgiye.public.blob.vercel-storage.com/demo/bouncing-ball.cpp'],localPath:'/compiler-assets/Demo_files/bouncing_ball.cpp'}),'shooter-game':Object.freeze({urls:['https://r2-public-assets.albatrossc.workers.dev/demo/shooter_game.cpp','https://ltjlklxc9homgiye.public.blob.vercel-storage.com/demo/shooter-game.cpp'],localPath:'/compiler-assets/Demo_files/shooter_game.cpp'})})});const compilerAssetHealthCache=new Map();function getCompilerAsset(category,resourceId){return COMPILER_ASSET_SOURCES?.[category]?.[resourceId]||null;}
function getCompilerAssetCandidateUrls(category,resourceId,options={}){const asset=getCompilerAsset(category,resourceId);if(!asset)return[];const preferLocal=options.preferLocal===true||!navigator.onLine;let remoteUrls=Array.isArray(asset.urls)?[...asset.urls]:[];const localUrl=asset.localPath||null;if(window.PUBLIC_ASSETS_URL){remoteUrls=remoteUrls.map(url=>{if(url.includes('r2-public-assets.albatrossc.workers.dev')){return url.replace('https://r2-public-assets.albatrossc.workers.dev',window.PUBLIC_ASSETS_URL);}
return url;});}else{if(category!=='libs'){remoteUrls=[];}}
return preferLocal?[localUrl,...remoteUrls].filter(Boolean):[...remoteUrls,localUrl].filter(Boolean);}
async function isCompilerAssetUrlReachable(url,timeout=3000){if(!url)return false;if(url.startsWith('/'))return true;if(!navigator.onLine)return false;const now=Date.now();const cached=compilerAssetHealthCache.get(url);if(cached&&(now-cached.timestamp)<300000){return cached.ok;}
try{const controller=new AbortController();const timeoutId=setTimeout(()=>controller.abort(),timeout);const response=await fetch(url,{method:'HEAD',cache:'default',signal:controller.signal});clearTimeout(timeoutId);const ok=response.ok;compilerAssetHealthCache.set(url,{ok,timestamp:now});return ok;}catch(error){compilerAssetHealthCache.set(url,{ok:false,timestamp:now});return false;}}
async function resolveCompilerAssetUrl(category,resourceId,options={}){const candidateUrls=getCompilerAssetCandidateUrls(category,resourceId,options);const asset=getCompilerAsset(category,resourceId);const localUrl=asset?.localPath||null;for(const url of candidateUrls){if(!url)continue;if(url.startsWith('/')){return url;}
if(await isCompilerAssetUrlReachable(url,options.timeout||3000)){return url;}}
return localUrl;}
async function fetchCompilerAsset(category,resourceId,options={}){const asset=getCompilerAsset(category,resourceId);if(!asset){throw new Error(`Unknown compiler asset: ${category}/${resourceId}`);}
const candidateUrls=getCompilerAssetCandidateUrls(category,resourceId,options);let lastError=null;for(const url of candidateUrls){if(!url)continue;try{const response=await fetch(url,options.fetchOptions||{});if(response.ok){return response;}
lastError=new Error(`Failed to fetch ${resourceId} (HTTP ${response.status})`);}catch(error){lastError=error;}}
throw lastError||new Error(`Failed to fetch compiler asset: ${category}/${resourceId}`);}
async function loadCompilerScript(category,resourceId,timeout=5000){const url=await resolveCompilerAssetUrl(category,resourceId,{timeout});if(!url){throw new Error(`Unable to resolve script: ${category}/${resourceId}`);}
return new Promise((resolve,reject)=>{const script=document.createElement('script');const timer=setTimeout(()=>{script.onload=null;script.onerror=null;reject(new Error(`Timeout loading script: ${url}`));},timeout);script.src=url;script.onload=()=>{clearTimeout(timer);resolve(url);};script.onerror=()=>{clearTimeout(timer);reject(new Error(`Failed to load script: ${url}`));};document.head.appendChild(script);});}
function getCompilerDemoFiles(){const demoEntries=Object.entries(COMPILER_ASSET_SOURCES.demos||{});const demos={};for(const[key,asset]of demoEntries){if(!navigator.onLine||!window.PUBLIC_ASSETS_URL){demos[key]=asset.localPath;}else{const original=asset.urls.find(u=>u.includes('r2-public-assets.albatrossc.workers.dev'));demos[key]=original?original.replace('https://r2-public-assets.albatrossc.workers.dev',window.PUBLIC_ASSETS_URL):asset.urls[0];}}
return demos;};const SETTINGS_STORAGE_KEY='editor_settings';const LEGACY_FONT_STORAGE_KEY='editor_font_size';const APP_SETTINGS_DEFAULTS=Object.freeze({editor:{fontSize:16,wordWrap:true,lineNumbers:true,bracketMatching:true,activeLine:true,autocomplete:true,hoverTooltips:true}});function cloneDefaultSettings(){return{editor:{...APP_SETTINGS_DEFAULTS.editor}};}
function clampFontSize(value){const parsed=Number.parseInt(value,10);if(Number.isNaN(parsed))return APP_SETTINGS_DEFAULTS.editor.fontSize;return Math.max(10,Math.min(32,parsed));}
function normalizeAppSettings(rawSettings){const base=cloneDefaultSettings();const raw=(rawSettings&&typeof rawSettings==='object')?rawSettings:{};const legacyFontSize=localStorage.getItem(LEGACY_FONT_STORAGE_KEY);const rawEditor=raw.editor&&typeof raw.editor==='object'?raw.editor:{};return{editor:{fontSize:clampFontSize(rawEditor.fontSize??raw.fontSize??legacyFontSize),wordWrap:rawEditor.wordWrap??raw.wordWrap??base.editor.wordWrap,lineNumbers:rawEditor.lineNumbers??raw.lineNumbers??base.editor.lineNumbers,bracketMatching:rawEditor.bracketMatching??raw.bracketMatching??base.editor.bracketMatching,activeLine:rawEditor.activeLine??raw.activeLine??base.editor.activeLine,autocomplete:rawEditor.autocomplete??raw.autocomplete??base.editor.autocomplete,hoverTooltips:rawEditor.hoverTooltips??raw.hoverTooltips??base.editor.hoverTooltips}};}
function loadAppSettings(){try{const serialized=localStorage.getItem(SETTINGS_STORAGE_KEY);if(!serialized){return normalizeAppSettings({});}
return normalizeAppSettings(JSON.parse(serialized));}catch(error){return normalizeAppSettings({});}}
function saveAppSettings(settings){const normalized=normalizeAppSettings(settings);localStorage.setItem(SETTINGS_STORAGE_KEY,JSON.stringify(normalized));localStorage.removeItem(LEGACY_FONT_STORAGE_KEY);return normalized;}
const Logger={prefix:'[Graphics.h Compiler]',colors:{info:'#7c8df0',success:'#6ac47b',error:'#cc4444',warn:'#ffb454',debug:'#888888'},timestamp(){return new Date().toISOString();},format(level,msg){return`${this.prefix} ${this.timestamp()} [${level}] ${msg}`;},info(msg){const formatted=this.format('INFO',msg);console.log(`%c${formatted}`,`color: ${this.colors.info}; font-weight: bold;`);},success(msg){const formatted=this.format('OK',msg);console.log(`%c${formatted}`,`color: ${this.colors.success}; font-weight: bold;`);},error(msg,err){const formatted=this.format('ERROR',msg);console.error(`%c${formatted}`,`color: ${this.colors.error}; font-weight: bold;`,err||'');},warn(msg){const formatted=this.format('WARN',msg);console.warn(`%c${formatted}`,`color: ${this.colors.warn}; font-weight: bold;`);},debug(msg){const formatted=this.format('DEBUG',msg);console.log(`%c${formatted}`,`color: ${this.colors.debug};`);}};const metrics={editor:{changeCount:0,lastChangeAt:null},runtime:{runCount:0,zipExtractionStarted:0,zipExtractionCompleted:0}};let dosInstance=null;let terminalFocused=false;let editor=null;let lastLoadedDemo='';let scriptsLoaded={codemirror:false};const CLOUD_STATE={files:new Map(),folders:new Set(['root']),folderNameToId:new Map(),hashToFileKey:new Map(),openTabs:[],activeFileKey:'root/main.cpp',autosaveTimer:null,isSaving:false,lastSavedHash:null,lastSavedAt:null};const isMobile=/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)||window.innerWidth<=768;const AUTOSAVE_DELAY_MS=20000;const TYPING_DEBOUNCE_MS=0;let DEMO_FILES={};let DEMO_FILE_CONTENTS={};let demoBundlePromise=null;let compilerAssetsInitPromise=null;async function initializeResourcesFromManifest(){if(compilerAssetsInitPromise){return compilerAssetsInitPromise;}
compilerAssetsInitPromise=(async()=>{try{DEMO_FILES=getCompilerDemoFiles();Logger.success('Compiler asset sources initialized');}catch(error){Logger.error('Failed to initialize compiler assets',error);DEMO_FILES={'graphics-demo':'/compiler-assets/Demo_files/graphics_demo.cpp','circle-pattern':'/compiler-assets/Demo_files/circle_pattern.cpp','bouncing-ball':'/compiler-assets/Demo_files/bouncing_ball.cpp','shooter-game':'/compiler-assets/Demo_files/shooter_game.cpp'};}})();return compilerAssetsInitPromise;}
async function loadDemoBundle(){if(demoBundlePromise)return demoBundlePromise;demoBundlePromise=(async()=>{const response=await fetchCompilerAsset('assets','demo-files-v1',{fetchOptions:{cache:'default'}});if(!response.ok){throw new Error(`Failed to load demo bundle (HTTP ${response.status})`);}
const payload=await response.json();const files=Array.isArray(payload?.files)?payload.files:[];DEMO_FILE_CONTENTS={};for(const file of files){if(!file?.demo_key||typeof file.code_content!=='string')continue;DEMO_FILE_CONTENTS[file.demo_key]=file.code_content;if(file.file_name){DEMO_FILES[file.demo_key]=file.file_name;}}
Logger.success(`Demo bundle loaded (${files.length} demos)`);return DEMO_FILE_CONTENTS;})();return demoBundlePromise;}
const CACHE_CONFIG={DEMO_CACHE_PREFIX:'demo_cache_',CACHE_TTL:7*24*60*60*1000,COMPILER_CACHE_NAME:'graphics-h-compiler-runtime-v1',JSDOS_RUNTIME_URL:'/libs/js-dos.js',WDOSBOX_SCRIPT_URL:'/libs/wdosbox.js',PRELOAD_WASM_URL:'/libs/wdosbox.wasm',DOS_RUNNER_URL:'/static/dos-runner.html'};const compilerFetchPromises=new Map();let preloadStarted=false;let preloadPromise=null;let dosRunnerFramePromise=null;async function openCompilerCache(){if(typeof caches==='undefined'){return null;}
try{return await caches.open(CACHE_CONFIG.COMPILER_CACHE_NAME);}catch(error){Logger.warn(`Cache API unavailable: ${error.message}`);return null;}}
function normalizeCacheUrl(url){return new URL(url,window.location.origin).toString();}
async function getCachedResponse(url){const cache=await openCompilerCache();if(!cache)return null;return cache.match(normalizeCacheUrl(url));}
async function hasCachedCompilerAsset(category,resourceId){const candidateUrls=getCompilerAssetCandidateUrls(category,resourceId,{preferLocal:!navigator.onLine});for(const url of candidateUrls){if(await getCachedResponse(url)){return true;}}
return false;}
async function cachedFetch(url,fetchOptions={}){const normalizedUrl=normalizeCacheUrl(url);const existingPromise=compilerFetchPromises.get(normalizedUrl);if(existingPromise){return(await existingPromise).clone();}
const requestPromise=(async()=>{const cached=await getCachedResponse(normalizedUrl);if(cached){Logger.info(`Cache hit: ${normalizedUrl}`);return cached;}
Logger.info(`Fetching asset: ${normalizedUrl}`);const response=await fetch(normalizedUrl,{cache:'default',...fetchOptions});if(!response.ok){throw new Error(`Failed to fetch ${normalizedUrl} (HTTP ${response.status})`);}
const blob=await response.blob();if(!blob.size){throw new Error(`Refusing to cache empty response for ${normalizedUrl}`);}
const cacheableResponse=new Response(blob,{status:response.status,statusText:response.statusText,headers:response.headers});const cache=await openCompilerCache();if(cache){await cache.put(normalizedUrl,cacheableResponse.clone());}
return cacheableResponse;})().finally(()=>{compilerFetchPromises.delete(normalizedUrl);});compilerFetchPromises.set(normalizedUrl,requestPromise);return(await requestPromise).clone();}
async function cachedFetchCompilerAsset(category,resourceId,options={}){const candidateUrls=getCompilerAssetCandidateUrls(category,resourceId,options);let lastError=null;for(const url of candidateUrls){try{return await cachedFetch(url,options.fetchOptions||{});}catch(error){lastError=error;}}
throw lastError||new Error(`Failed to fetch compiler asset: ${category}/${resourceId}`);}
const DemoCache={get(demoKey){try{const cacheKey=CACHE_CONFIG.DEMO_CACHE_PREFIX+demoKey;const cached=localStorage.getItem(cacheKey);if(!cached)return null;const{code,timestamp}=JSON.parse(cached);const age=Date.now()-timestamp;if(age<CACHE_CONFIG.CACHE_TTL){return code;}
localStorage.removeItem(cacheKey);return null;}catch(e){return null;}},set(demoKey,code){try{const cacheKey=CACHE_CONFIG.DEMO_CACHE_PREFIX+demoKey;localStorage.setItem(cacheKey,JSON.stringify({code:code,timestamp:Date.now()}));return true;}catch(e){Logger.warn('Failed to cache demo file: '+e.message);return false;}}};let tcZipPromise=null;async function getTCZip(){await initializeResourcesFromManifest();if(tcZipPromise)return tcZipPromise;tcZipPromise=(async()=>{try{const response=await cachedFetchCompilerAsset('assets','tc-zip');const blob=await response.blob();Logger.success('TC ZIP ready');return blob;}catch(error){tcZipPromise=null;throw error;}})();return tcZipPromise;}
async function startPreload(){if(preloadStarted&&preloadPromise){return preloadPromise;}
preloadStarted=true;preloadPromise=(async()=>{await initializeResourcesFromManifest();Logger.info('Starting compiler preload sequence...');await cachedFetch(CACHE_CONFIG.JSDOS_RUNTIME_URL);await cachedFetch(CACHE_CONFIG.WDOSBOX_SCRIPT_URL);await cachedFetch(CACHE_CONFIG.PRELOAD_WASM_URL);await cachedFetchCompilerAsset('assets','tc-zip');Logger.success('Compiler preload finished');})().catch((error)=>{preloadStarted=false;preloadPromise=null;throw error;});return preloadPromise;}
async function updateCacheStatus(){try{const cached=await hasCachedCompilerAsset('assets','tc-zip');if(cached&&runBtn){runBtn.title='Run this program [Ctrl+Enter]';Logger.info('Cache status: TC ZIP is cached');}else if(runBtn){runBtn.title='Run this program [Ctrl+Enter]';}}catch(e){}}
const loading=document.getElementById("loading");const loadingText=document.getElementById("loading-text");const loadingProgressBar=document.getElementById("loading-progress-bar");const editorLoadingText=document.getElementById("editor-loading-text");const editorLoadingSubtext=document.getElementById("editor-loading-subtext");const runBtn=document.getElementById("run-btn");const terminalWrapper=document.getElementById("terminal-wrapper");const keyboardBlocker=document.getElementById("keyboard-blocker");const editorWrapper=document.getElementById("editor-wrapper");const demoSelect=document.getElementById("demo-select");const clearBtn=document.getElementById("clear-btn");const editorInfo=document.getElementById("editor-info");const saveIndicator=document.getElementById("save-indicator");const saveText=document.getElementById("save-text");const terminalIdleState=document.getElementById("terminal-idle-state");function updateEditorLoadingState(title,detail){if(editorLoadingText&&title){editorLoadingText.textContent=title;}
if(editorLoadingSubtext&&detail){editorLoadingSubtext.textContent=detail;}}
function ensureDosRunnerFrame(){if(dosRunnerFramePromise){return dosRunnerFramePromise;}
const iframe=document.getElementById('dos-iframe');if(!iframe){return Promise.reject(new Error('DOS terminal iframe is missing.'));}
if(iframe.dataset.loaded==='true'&&iframe.src){dosRunnerFramePromise=Promise.resolve(iframe);return dosRunnerFramePromise;}
dosRunnerFramePromise=new Promise((resolve,reject)=>{const handleLoad=()=>{iframe.dataset.loaded='true';if(terminalIdleState){terminalIdleState.classList.add('hidden');}
resolve(iframe);};const handleError=()=>{dosRunnerFramePromise=null;reject(new Error('Failed to load DOS runner frame.'));};iframe.addEventListener('load',handleLoad,{once:true});iframe.addEventListener('error',handleError,{once:true});if(!iframe.src){iframe.src=iframe.dataset.src||CACHE_CONFIG.DOS_RUNNER_URL;}else if(iframe.dataset.loaded==='true'){handleLoad();}});return dosRunnerFramePromise;}
const outputPanel=document.getElementById("output-panel");const outputContent=document.getElementById("output-content");const closeOutputBtn=document.getElementById("close-output-btn");let lastErrorContent='';closeOutputBtn.addEventListener('click',()=>{outputPanel.classList.remove('visible');terminalWrapper.classList.remove('has-panel');window.dispatchEvent(new Event('resize'));});const copyErrorBtn=document.getElementById("copy-error-btn");const copyBtnText=document.getElementById("copy-btn-text");copyErrorBtn.addEventListener('click',async()=>{const errorText=outputContent.textContent;if(!errorText||errorText.trim()===''){return;}
try{await navigator.clipboard.writeText(errorText);copyErrorBtn.classList.add('copied');copyBtnText.textContent='Copied';Logger.success('Errors copied to clipboard');setTimeout(()=>{copyErrorBtn.classList.remove('copied');copyBtnText.textContent='Copy Errors';},2000);}catch(err){Logger.error('Failed to copy errors',err);const textArea=document.createElement('textarea');textArea.value=errorText;textArea.style.position='fixed';textArea.style.left='-9999px';document.body.appendChild(textArea);textArea.select();try{document.execCommand('copy');copyErrorBtn.classList.add('copied');copyBtnText.textContent='Copied';setTimeout(()=>{copyErrorBtn.classList.remove('copied');copyBtnText.textContent='Copy Errors';},2000);}catch(fallbackErr){Logger.error('Fallback copy failed',fallbackErr);}
document.body.removeChild(textArea);}});const expandOutputBtn=document.getElementById("expand-output-btn");let isOutputExpanded=false;expandOutputBtn.addEventListener('click',()=>{isOutputExpanded=!isOutputExpanded;if(isOutputExpanded){outputPanel.classList.add('expanded');expandOutputBtn.classList.add('expanded');expandOutputBtn.title='Collapse panel';}else{outputPanel.classList.remove('expanded');expandOutputBtn.classList.remove('expanded');expandOutputBtn.title='Expand panel';}
setTimeout(()=>window.dispatchEvent(new Event('resize')),310);Logger.info(`Output panel ${isOutputExpanded ? 'expanded' : 'collapsed'}`);});(function initSplitters(){const sidebar=document.getElementById('sidebar');const editorWrapper=document.getElementById('editor-wrapper');const terminalWrapper=document.getElementById('terminal-wrapper');const splitterSidebar=document.getElementById('splitter-sidebar');const splitterTerminal=document.getElementById('splitter-terminal');let isDragging=false;let currentSplitter=null;let startX=0;let startWidthSidebar=0;let startEditorFlex=0;let startTerminalFlex=0;function updateSidebarSplitterVisibility(){if(!splitterSidebar||!sidebar)return;const isCollapsed=sidebar.classList.contains('collapsed');splitterSidebar.style.display=isCollapsed?'none':'';}
if(sidebar){const observer=new MutationObserver(updateSidebarSplitterVisibility);observer.observe(sidebar,{attributes:true,attributeFilter:['class']});updateSidebarSplitterVisibility();}
function updateSplittersForFullscreen(){const anyFullscreen=document.querySelector('#editor-wrapper.fullscreen, #terminal-wrapper.fullscreen');const hide=Boolean(anyFullscreen);if(splitterSidebar)splitterSidebar.style.visibility=hide?'hidden':'';if(splitterTerminal)splitterTerminal.style.visibility=hide?'hidden':'';}
[editorWrapper,terminalWrapper].forEach(panel=>{if(!panel)return;const obs=new MutationObserver(updateSplittersForFullscreen);obs.observe(panel,{attributes:true,attributeFilter:['class']});});function startDrag(clientX,splitterType){isDragging=true;currentSplitter=splitterType;startX=clientX;if(splitterType==='sidebar'){startWidthSidebar=sidebar?sidebar.offsetWidth:0;if(splitterSidebar)splitterSidebar.classList.add('dragging');}else if(splitterType==='terminal'){startEditorFlex=editorWrapper?editorWrapper.getBoundingClientRect().width:1;startTerminalFlex=terminalWrapper?terminalWrapper.getBoundingClientRect().width:1;if(splitterTerminal)splitterTerminal.classList.add('dragging');}
document.body.style.cursor='col-resize';document.body.style.userSelect='none';const iframe=document.getElementById('dos-iframe');if(iframe)iframe.style.pointerEvents='none';}
function moveDrag(clientX){if(!isDragging)return;const dx=clientX-startX;if(currentSplitter==='sidebar'){const newWidth=startWidthSidebar+dx;if(newWidth<80){if(sidebar&&!sidebar.classList.contains('collapsed')){sidebar.classList.add('collapsed');sidebar.style.width='';sidebar.style.minWidth='';endDrag();}
return;}
const boundedWidth=Math.max(120,Math.min(600,newWidth));if(sidebar){sidebar.style.width=boundedWidth+'px';sidebar.style.minWidth=boundedWidth+'px';}}else if(currentSplitter==='terminal'){const newEditorW=startEditorFlex+dx;const newTerminalW=startTerminalFlex-dx;if(newEditorW<80||newTerminalW<80)return;const total=newEditorW+newTerminalW;if(editorWrapper)editorWrapper.style.flex=(newEditorW/total*4).toFixed(3)+' 1 0';if(terminalWrapper)terminalWrapper.style.flex=(newTerminalW/total*4).toFixed(3)+' 1 0';}}
function endDrag(){if(!isDragging)return;isDragging=false;currentSplitter=null;if(splitterSidebar)splitterSidebar.classList.remove('dragging');if(splitterTerminal)splitterTerminal.classList.remove('dragging');document.body.style.cursor='';document.body.style.userSelect='';document.removeEventListener('mousemove',onMouseMove);document.removeEventListener('mouseup',onMouseUp);document.removeEventListener('touchmove',onTouchMove);document.removeEventListener('touchend',onTouchEnd);const iframe=document.getElementById('dos-iframe');if(iframe)iframe.style.pointerEvents='';window.dispatchEvent(new Event('resize'));if(typeof editor!=='undefined'&&editor&&editor.requestMeasure){setTimeout(()=>editor.requestMeasure(),50);}}
function onMouseDown(e,splitterType){if(e.button!==0)return;startDrag(e.clientX,splitterType);document.addEventListener('mousemove',onMouseMove);document.addEventListener('mouseup',onMouseUp);e.preventDefault();}
function onMouseMove(e){moveDrag(e.clientX);}
function onMouseUp(){endDrag();}
function onTouchStart(e,splitterType){if(e.touches.length!==1)return;startDrag(e.touches[0].clientX,splitterType);document.addEventListener('touchmove',onTouchMove,{passive:false});document.addEventListener('touchend',onTouchEnd);e.preventDefault();}
function onTouchMove(e){if(e.touches.length!==1)return;moveDrag(e.touches[0].clientX);e.preventDefault();}
function onTouchEnd(){endDrag();}
function resetSidebarWidth(){if(!sidebar)return;sidebar.style.width='';sidebar.style.minWidth='';if(sidebar.classList.contains('collapsed')){sidebar.classList.remove('collapsed');}
window.dispatchEvent(new Event('resize'));if(typeof editor!=='undefined'&&editor&&editor.requestMeasure){setTimeout(()=>editor.requestMeasure(),50);}}
function resetPanelRatio(){if(editorWrapper)editorWrapper.style.flex='';if(terminalWrapper)terminalWrapper.style.flex='';window.dispatchEvent(new Event('resize'));if(typeof editor!=='undefined'&&editor&&editor.requestMeasure){setTimeout(()=>editor.requestMeasure(),50);}}
if(splitterSidebar){splitterSidebar.addEventListener('mousedown',(e)=>onMouseDown(e,'sidebar'));splitterSidebar.addEventListener('touchstart',(e)=>onTouchStart(e,'sidebar'),{passive:false});splitterSidebar.addEventListener('dblclick',resetSidebarWidth);}
if(splitterTerminal){splitterTerminal.addEventListener('mousedown',(e)=>onMouseDown(e,'terminal'));splitterTerminal.addEventListener('touchstart',(e)=>onTouchStart(e,'terminal'),{passive:false});splitterTerminal.addEventListener('dblclick',resetPanelRatio);}})();;(function(){'use strict';const progressBar=document.getElementById('progress-bar');const progressFill=document.getElementById('progress-fill');let progressDepth=0;function showProgress(){if(!progressBar||!progressFill)return;progressDepth+=1;progressBar.classList.remove('hidden');progressFill.classList.add('indeterminate');}
function hideProgress(){if(!progressBar||!progressFill)return;progressDepth=Math.max(0,progressDepth-1);if(progressDepth>0)return;progressFill.classList.remove('indeterminate');progressBar.classList.add('hidden');}
function setExplorerLoading(isLoading,text){const loading=document.getElementById('explorer-loading-state');const loadingText=document.getElementById('explorer-loading-text');const fileList=document.getElementById('main-folder-files');if(!loading||!fileList)return;if(loadingText)loadingText.textContent=text||'Loading files...';loading.classList.toggle('hidden',!isLoading);fileList.classList.toggle('hidden',isLoading);}
function updateSaveIndicator(){if(!saveIndicator||!saveText)return;if(!DIRTY_FLAG.isDirty&&SAVE_STATE.lastSavedHash){saveIndicator.classList.add('saved');saveText.textContent=isUserLoggedIn?'Saved to cloud':'Saved';return;}
saveIndicator.classList.remove('saved');saveText.textContent='Unsaved';}
function getFileIconClass(ext){if(ext==='cpp'||ext==='c')return'icon-cpp';if(ext==='h'||ext==='hpp')return'icon-header';if(ext==='txt')return'icon-text';return'icon-default';}
function createFileItem(folder,filename){const item=document.createElement('div');const ext=filename.split('.').pop().toLowerCase();item.className='file-item';item.dataset.folder=folder;item.dataset.file=filename;if(fileKey(folder,filename)===(CLOUD_STATE.activeFileKey||DEFAULT_FILE_KEY)){item.classList.add('active');}
item.innerHTML=`
        <svg class="file-icon ${getFileIconClass(ext)}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"></path><polyline points="14 2 14 8 20 8"></polyline></svg>
        <div class="file-main">
            <span class="file-name" title="${filename}">${filename}</span>
        </div>
        <div class="file-actions">
            <button class="file-action-btn file-download-btn" title="Download file" onclick="event.stopPropagation(); downloadFile('${folder}', '${filename}')"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"></path><polyline points="7 10 12 15 17 10"></polyline><line x1="12" y1="15" x2="12" y2="3"></line></svg></button>
            <button class="file-action-btn file-delete-btn" title="Delete file" onclick="event.stopPropagation(); deleteFile('${folder}', '${filename}')"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2M10 11v6M14 11v6"></path></svg></button>
        </div>`;item.addEventListener('click',()=>{setSelectedFolder(folder);openFile(folder,filename);const sidebar=document.getElementById('sidebar');const sidebarOverlay=document.getElementById('sidebar-overlay');if(sidebar&&sidebar.classList.contains('open')){sidebar.classList.remove('open');if(sidebarOverlay)sidebarOverlay.classList.remove('active');if(typeof window.syncMobileSidebarState==='function'){window.syncMobileSidebarState();}}
if(typeof window.switchMobileTab==='function'){window.switchMobileTab('editor');}});return item;}
function createFolderGroup(folder,name,files){const collapsed=isFolderCollapsed(folder);const group=document.createElement('div');group.className='folder-group';group.classList.toggle('collapsed',collapsed);group.dataset.folder=folder;const header=document.createElement('div');header.className='folder-group-header';header.setAttribute('role','button');header.setAttribute('aria-expanded',String(!collapsed));if((CLOUD_STATE.selectedFolderKey||ROOT_FOLDER_KEY)===folder){header.classList.add('selected');}
const fileCountLabel=`${files.length} file${files.length === 1 ? '' : 's'}`;header.innerHTML=`<span class="folder-group-title"><span class="folder-chevron" aria-hidden="true">▾</span><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"></path></svg><span>${name}</span></span><span class="folder-file-count">${fileCountLabel}</span>`;header.addEventListener('click',()=>{setSelectedFolder(folder);setFolderCollapsed(folder,!collapsed);renderFileExplorer();});if(folder!==ROOT_FOLDER_KEY){const button=document.createElement('button');button.className='folder-action-btn';button.title=`Delete folder ${name}`;button.innerHTML=`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path></svg>`;button.addEventListener('click',(event)=>{event.stopPropagation();deleteFolder(folder,name).catch((error)=>alert(error.message));});header.appendChild(button);}
group.appendChild(header);const wrap=document.createElement('div');wrap.className='folder-group-files';if(!files.length){const empty=document.createElement('div');empty.className='folder-group-empty';empty.textContent='No files';wrap.appendChild(empty);}else{files.forEach((file)=>wrap.appendChild(createFileItem(folder,file.filename)));}
group.appendChild(wrap);return group;}
function renderFileExplorer(){const fileList=document.getElementById('main-folder-files');const filesCount=document.getElementById('files-count');if(!fileList)return;if(!CLOUD_STATE.folderIdToName)setDefaultFolderState();const filesByFolder=new Map();for(const[id,name]of CLOUD_STATE.folderIdToName.entries()){if(!filesByFolder.has(id))filesByFolder.set(id,[]);CLOUD_STATE.folders.add(id);CLOUD_STATE.folderNameToId.set(name,id);}
CLOUD_STATE.files.forEach((file)=>{if(!isVisibleCloudFile(file))return;const key=file.folder_key||ROOT_FOLDER_KEY;if(!filesByFolder.has(key))filesByFolder.set(key,[]);filesByFolder.get(key).push(file);});filesByFolder.forEach((files)=>files.sort((a,b)=>a.filename.localeCompare(b.filename)));const orderedFolders=Array.from(CLOUD_STATE.folderIdToName.keys()).sort((a,b)=>getFolderName(a).localeCompare(getFolderName(b)));fileList.innerHTML='';const fragment=document.createDocumentFragment();orderedFolders.forEach((key)=>{fragment.appendChild(createFolderGroup(key,getFolderName(key),filesByFolder.get(key)||[]));});if(!fragment.childNodes.length){const empty=document.createElement('div');empty.className='file-list-empty';empty.textContent='No files yet. Create a file to get started.';fragment.appendChild(empty);}
fileList.appendChild(fragment);const visibleCount=getVisibleCloudFiles().length;if(filesCount){filesCount.textContent=`${visibleCount} file${visibleCount === 1 ? '' : 's'}`;}}
function highlightActiveFile(){const[folder,filename]=(CLOUD_STATE.activeFileKey||DEFAULT_FILE_KEY).split('/');document.querySelectorAll('.file-item').forEach((item)=>{item.classList.toggle('active',item.dataset.folder===folder&&item.dataset.file===filename);});document.querySelectorAll('.folder-group-header').forEach((header)=>{header.classList.toggle('selected',header.parentElement?.dataset.folder===(CLOUD_STATE.selectedFolderKey||ROOT_FOLDER_KEY));});const tab=document.getElementById('current-file-tab');const name=document.getElementById('current-file-name');if(tab)tab.dataset.file=filename;if(name)name.textContent=filename;}
window.showProgress=showProgress;window.hideProgress=hideProgress;window.setExplorerLoading=setExplorerLoading;window.updateSaveIndicator=updateSaveIndicator;window.renderFileExplorer=renderFileExplorer;window.highlightActiveFile=highlightActiveFile;})();;let isUserLoggedIn=false;let currentUser=null;let authConfig=null;let isSaving=false;let googleIdentityReady=false;let googleIdentityInitPromise=null;const SAVE_STATE={lastSavedHash:null,pendingHash:null,lastSaveTime:0};const DIRTY_FLAG={isDirty:false};const LEGACY_LOCAL_DRAFTS_KEY='compiler_cloud_drafts_v1';const FOLDER_UI_STATE_KEY='compiler_folder_ui_state_v1';const ROOT_FOLDER_KEY='root';const DEFAULT_FILE_NAME='main.cpp';const DEFAULT_FILE_KEY=`${ROOT_FOLDER_KEY}/${DEFAULT_FILE_NAME}`;const DEFAULT_SOURCE=`#include <graphics.h>\n#include <conio.h>\n\nint main()\n{\n    int gd = DETECT, gm;\n    initgraph(&gd, &gm, "");\n\n    // Your code here\n\n    getch();\n    closegraph();\n    return 0;\n}\n`;const DEMO_HASHES=new Set(['8392b1554b6b9643fdedfeb898f177333b7b04f8565de621b57abb383d51b964','a7bf3145a7ef0b22a466143545d4622d9fc16fc94a923f3b140658b70135f3fc','617e8525ac300822d4410e0b53d40fa201afde96548fa73b0597d77395c46dab','20c59b432fd12667ff76b11202cb1ef97c6cd24690b0dc1d42ff35c6cdd519a4','989948aff97363700ec404db511d7564a01a3caa94b48ad827d9d5f818ebe6ad',]);function folderKey(folderId){return folderId||ROOT_FOLDER_KEY;}
function folderId(value){return!value||value===ROOT_FOLDER_KEY?null:value;}
function fileKey(folder,filename){return`${folderKey(folder)}/${filename}`;}
function activeFileInfo(){const[folder,filename]=(CLOUD_STATE.activeFileKey||DEFAULT_FILE_KEY).split('/');return{folder,folderId:folderId(folder),filename,key:CLOUD_STATE.activeFileKey||DEFAULT_FILE_KEY};}
function authStatusEl(){return document.getElementById('auth-status-text');}
function setAuthStatus(text){const el=authStatusEl();if(!el)return;if(text==='Unlimited projects · Access anywhere'||text==='Unlimited projects · Cloud synced'||text===''){el.textContent='';el.style.display='none';}else{el.textContent=text;el.style.display='block';}}
function setDefaultFolderState(){CLOUD_STATE.folders=new Set([ROOT_FOLDER_KEY]);CLOUD_STATE.folderNameToId=new Map();CLOUD_STATE.folderIdToName=new Map();CLOUD_STATE.selectedFolderKey=CLOUD_STATE.selectedFolderKey||ROOT_FOLDER_KEY;}
function getFolderName(idOrKey){return!idOrKey||idOrKey===ROOT_FOLDER_KEY?'':(CLOUD_STATE.folderIdToName.get(idOrKey)||'Folder');}
function computeBytes(text){return new TextEncoder().encode(text||'').byteLength;}
async function computeSha256(content){const digest=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(content));return Array.from(new Uint8Array(digest)).map((b)=>b.toString(16).padStart(2,'0')).join('');}
const FILE_DB_NAME='compiler_project_files_v1';const FILE_DB_VERSION=1;const FILE_DB_STORE='files';const FileDB={_db:null,async open(){if(this._db)return this._db;return new Promise((resolve,reject)=>{const timeoutId=setTimeout(()=>{reject(new Error('IndexedDB open timed out — storage may be unavailable'));},3000);let req;try{req=indexedDB.open(FILE_DB_NAME,FILE_DB_VERSION);}catch(e){clearTimeout(timeoutId);reject(e);return;}
req.onupgradeneeded=(e)=>{const db=e.target.result;if(!db.objectStoreNames.contains(FILE_DB_STORE)){db.createObjectStore(FILE_DB_STORE,{keyPath:'id'});}};req.onsuccess=(e)=>{clearTimeout(timeoutId);this._db=e.target.result;resolve(this._db);};req.onerror=(e)=>{clearTimeout(timeoutId);reject(e.target.error);};req.onblocked=()=>{clearTimeout(timeoutId);reject(new Error('IndexedDB open blocked by another connection'));};});},async getAll(){try{const db=await this.open();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_DB_STORE,'readonly');const r=tx.objectStore(FILE_DB_STORE).getAll();r.onsuccess=()=>resolve(r.result||[]);r.onerror=()=>reject(r.error);});}catch(e){Logger.warn('[FileDB] getAll: '+e.message);return[];}},async get(id){try{const db=await this.open();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_DB_STORE,'readonly');const r=tx.objectStore(FILE_DB_STORE).get(id);r.onsuccess=()=>resolve(r.result||null);r.onerror=()=>reject(r.error);});}catch(e){Logger.warn('[FileDB] get: '+e.message);return null;}},async put(record){try{const db=await this.open();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_DB_STORE,'readwrite');const r=tx.objectStore(FILE_DB_STORE).put(record);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error);});}catch(e){Logger.warn('[FileDB] put: '+e.message);}},async delete(id){try{const db=await this.open();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_DB_STORE,'readwrite');const r=tx.objectStore(FILE_DB_STORE).delete(id);r.onsuccess=()=>resolve();r.onerror=()=>reject(r.error);});}catch(e){Logger.warn('[FileDB] delete: '+e.message);}},async clear(){try{const db=await this.open();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_DB_STORE,'readwrite');const r=tx.objectStore(FILE_DB_STORE).clear();r.onsuccess=()=>resolve();r.onerror=()=>reject(r.error);});}catch(e){Logger.warn('[FileDB] clear: '+e.message);}}};async function setLocalDraft(folder,filename,content){await FileDB.put({id:fileKey(folder,filename),name:filename,content,lastSavedHash:SAVE_STATE.lastSavedHash||null,lastModified:Date.now(),dirty:true,folderId:folderId(folder),folderKey:folderKey(folder)});}
function setLocalDraftImmediate(folder,filename,content){setLocalDraft(folder,filename,content).catch(()=>{});}
async function getLocalDraft(folder,filename){const record=await FileDB.get(fileKey(folder,filename));return record?record.content:null;}
async function clearLocalDraft(folder,filename){await FileDB.delete(fileKey(folder,filename));}
async function clearAllLocalDrafts(){await FileDB.clear();localStorage.removeItem(LEGACY_LOCAL_DRAFTS_KEY);}
let folderUiStateCache=null;function loadFolderUiState(){if(folderUiStateCache)return folderUiStateCache;try{const parsed=JSON.parse(localStorage.getItem(FOLDER_UI_STATE_KEY)||'{}');folderUiStateCache=parsed&&typeof parsed==='object'?parsed:{};}catch{folderUiStateCache={};}
return folderUiStateCache;}
function saveFolderUiState(state){folderUiStateCache=state;localStorage.setItem(FOLDER_UI_STATE_KEY,JSON.stringify(state));}
function isFolderCollapsed(folder){const state=loadFolderUiState();return state[folder]===true;}
function setFolderCollapsed(folder,collapsed){const state={...loadFolderUiState(),[folder]:collapsed===true};saveFolderUiState(state);}
function setSelectedFolder(key){CLOUD_STATE.selectedFolderKey=key||ROOT_FOLDER_KEY;}
function isVisibleCloudFile(file){const id=file?.folder_id;return id!==null&&id!==undefined&&id!=='';}
function getVisibleCloudFiles(){return Array.from(CLOUD_STATE.files.values()).filter((file)=>isVisibleCloudFile(file));}
function getFirstVisibleCloudFile(){return getVisibleCloudFiles()[0]||null;}
function remoteFileByName(targetFolderId,filename){for(const file of CLOUD_STATE.files.values())if((file.folder_id||null)===(targetFolderId||null)&&file.filename===filename)return file;return null;}
function getMainCppFile(){for(const file of CLOUD_STATE.files.values()){if(file.filename==='main.cpp')return file;}
return null;}
function getFirstCloudFile(){return Array.from(CLOUD_STATE.files.values())[0]||null;}
function getCloudFileById(fileId){if(!fileId)return null;for(const file of CLOUD_STATE.files.values()){if(file?.id===fileId)return file;}
return null;}
function buildStarterSource(filename){const safeName=(filename||DEFAULT_FILE_NAME).trim()||DEFAULT_FILE_NAME;return`// ${safeName}\n#include <graphics.h>\n#include <conio.h>\n\nint main() {\n    int gd = DETECT, gm;\n    initgraph(&gd, &gm, "");\n    \n    // Your code here\n    \n    getch();\n    closegraph();\n    return 0;\n}\n`;}
function findFolderIdByName(name){const target=String(name||'').trim().toLowerCase();if(!target)return null;for(const[id,folderName]of CLOUD_STATE.folderIdToName.entries()){if(String(folderName||'').trim().toLowerCase()===target)return id;}
return null;}
async function ensureMainFolder(){const existing=findFolderIdByName('main');if(existing)return existing;const{response,payload}=await fetchJson('/api/folder/create',{method:'POST',body:JSON.stringify({folder_name:'main'})});if(!response.ok){if(response.status===409){await refreshCloudFiles(true);const refreshed=findFolderIdByName('main');if(refreshed)return refreshed;}
throw new Error(payload?.error||'Failed to create main folder');}
CLOUD_STATE.folderIdToName.set(payload.id,payload.folder_name);CLOUD_STATE.folderNameToId.set(payload.folder_name,payload.id);CLOUD_STATE.folders.add(payload.id);return payload.id;}
function userForUi(user){return{email:user.email||'',display_name:user.name||user.email||'Account',avatar_url:user.image||'',user_metadata:{full_name:user.name||user.email||'Account',email:user.email||'',avatar_url:user.image||''}};}
function safeUpdateLoginUI(loggedIn,user=null){if(typeof updateLoginUI==='function'){updateLoginUI(loggedIn,user);return true;}
isUserLoggedIn=loggedIn;currentUser=user;return false;}
async function readJsonSafe(response){const contentType=response.headers.get('Content-Type')||'';let rawText='';try{rawText=await response.text();}catch{return{payload:null,rawText:'',contentType};}
if(!rawText){return{payload:null,rawText:'',contentType};}
const trimmed=rawText.trim();const looksJson=contentType.includes('application/json')||trimmed.startsWith('{')||trimmed.startsWith('[');if(!looksJson){return{payload:null,rawText,contentType};}
try{return{payload:JSON.parse(rawText),rawText,contentType};}catch{return{payload:null,rawText,contentType};}}
async function fetchJson(url,options={}){const method=String(options.method||'GET').toUpperCase();const fetchOptions={credentials:'same-origin',...options,headers:{...(options.headers||{}),...(options.body?{'Content-Type':'application/json'}:{})}};if(method==='GET'&&!Object.prototype.hasOwnProperty.call(fetchOptions,'cache')){fetchOptions.cache='no-cache';}
const response=await fetch(url,fetchOptions);const{payload,rawText,contentType}=await readJsonSafe(response.clone());return{response,payload,rawText,contentType};}
function normalizeSessionUser(payload){const directEmail=payload?.email||payload?.user_email||payload?.mail||'';const directName=payload?.display_name||payload?.displayName||payload?.name||'';const directAvatar=payload?.avatar_url||payload?.picture||'';if(payload?.authenticated){const email=String(directEmail||'').trim();const name=String(directName||email||'').trim();const image=String(directAvatar||'').trim();if(email||name){return{email,name:name||email,image};}}
const user=payload?.user||payload?.session?.user||payload?.data?.user;if(!user)return null;const email=String(user.email||user.user_email||user.mail||'').trim();const name=String(user.name||user.display_name||user.displayName||email||'').trim();const image=user.image||user.avatar_url||user.picture||'';if(!email&&!name)return null;return{email,name:name||email,image};}
async function loadAuthConfig(){if(authConfig)return authConfig;const response=await fetch('/api/auth/config',{credentials:'same-origin',cache:'no-cache'});if(!response.ok)throw new Error('Failed to load auth config');authConfig=await response.json();return authConfig;}
async function checkSession(){try{const{response,payload}=await fetchJson('/api/auth/session',{method:'GET'});const user=response.ok?normalizeSessionUser(payload):null;if(!user){safeUpdateLoginUI(false);setAuthStatus('Unlimited projects · Access anywhere');return false;}
safeUpdateLoginUI(true,userForUi(user));setAuthStatus(`Signed in as ${user.email}`);return true;}catch(error){Logger.warn(`[Auth] ${error.message}`);safeUpdateLoginUI(false);setAuthStatus('Sign in is temporarily unavailable');return false;}}
function toCloudSnapshot(payload){return{last_opened_file_id:payload?.last_opened_file_id||payload?.data?.last_opened_file_id||null,folders:Array.isArray(payload?.folders)?payload.folders:(Array.isArray(payload?.data?.folders)?payload.data.folders:[]),files:Array.isArray(payload?.files)?payload.files.slice():(Array.isArray(payload?.data?.files)?payload.data.files.slice():[]),};}
async function fetchCloudSnapshot(){const{response,payload}=await fetchJson('/api/files',{method:'GET'});if(!response.ok)throw new Error(payload?.error||'Failed to load files');return toCloudSnapshot(payload);}
async function guestCodeModified(){const code=editor?editor.getValue():'';const hash=await computeSha256(code);return!DEMO_HASHES.has(hash);}
function findMainFolderIdInSnapshot(snapshot){const folders=Array.isArray(snapshot?.folders)?snapshot.folders:[];const mainFolder=folders.find((folder)=>String(folder?.folder_name||'').trim().toLowerCase()==='main');return mainFolder?.id||null;}
async function ensureMainFolderIdForSnapshot(snapshot){const existing=findMainFolderIdInSnapshot(snapshot);if(existing)return existing;const{response,payload}=await fetchJson('/api/folder/create',{method:'POST',body:JSON.stringify({folder_name:'main'})});if(!response.ok){if(response.status===409){const refreshedSnapshot=await fetchCloudSnapshot();snapshot.folders=refreshedSnapshot.folders;snapshot.files=refreshedSnapshot.files;snapshot.last_opened_file_id=refreshedSnapshot.last_opened_file_id||null;const refreshedMain=findMainFolderIdInSnapshot(snapshot);if(refreshedMain)return refreshedMain;}
throw new Error(payload?.error||'Failed to create main folder');}
snapshot.folders.push({id:payload.id,folder_name:payload.folder_name||'main'});return payload.id;}
function nextUntitledFilename(snapshot,targetFolderId){const files=Array.isArray(snapshot?.files)?snapshot.files:[];let maxN=0;for(const file of files){if((file?.folder_id||null)!==(targetFolderId||null))continue;const name=file?.file_name||file?.filename||'';const match=/^untitled-(\d+)\.cpp$/i.exec(name);if(!match)continue;const value=Number(match[1]);if(Number.isFinite(value)&&value>maxN)maxN=value;}
return`untitled-${maxN + 1}.cpp`;}
function getSnapshotFileById(snapshot,fileId){if(!fileId)return null;const files=Array.isArray(snapshot?.files)?snapshot.files:[];for(const file of files){if(file?.id===fileId)return file;}
return null;}
async function saveGuestCodeWithName(snapshot,content,fileName){const mainFolderId=await ensureMainFolderIdForSnapshot(snapshot);const{response,payload}=await fetchJson('/api/file/save',{method:'POST',body:JSON.stringify({folder_id:mainFolderId,file_name:fileName,content})});if(!response.ok)throw new Error(payload?.error||'Failed to save guest code');const folderName=(snapshot.folders.find((folder)=>folder?.id===mainFolderId)?.folder_name)||'main';snapshot.last_opened_file_id=payload?.file_id||snapshot.last_opened_file_id||null;snapshot.files.push({id:payload?.file_id||null,file_name:fileName,file_content:content,folder_id:mainFolderId,folder_name:folderName,file_size:payload?.file_size??computeBytes(content),content_hash:payload?.content_hash||null});return{fileName,folderId:mainFolderId};}
function resolveDefaultOpenTarget(snapshot){const files=Array.isArray(snapshot?.files)?snapshot.files:[];const active=getSnapshotFileById(snapshot,snapshot.last_opened_file_id)||files.find((f)=>(f?.file_name||f?.filename)==='main.cpp')||files[0]||null;if(!active)return null;return{folder:folderKey(active.folder_id||null),filename:active.file_name||active.filename};}
function showToast(message,durationMs=3500){let toast=document.getElementById('goc-signin-toast');if(!toast){toast=document.createElement('div');toast.id='goc-signin-toast';toast.className='fix-toast';toast.setAttribute('role','status');toast.setAttribute('aria-live','polite');document.body.appendChild(toast);}
toast.innerHTML=`
        <svg class="fix-toast-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="color:var(--primary)">
            <polyline points="20 6 9 17 4 12"></polyline>
        </svg>
        <span class="fix-toast-text">${message}</span>`;if(toast._hideTimer)clearTimeout(toast._hideTimer);toast.classList.remove('fix-toast-exit');requestAnimationFrame(()=>{requestAnimationFrame(()=>toast.classList.add('fix-toast-enter'));});toast._hideTimer=setTimeout(()=>{toast.classList.remove('fix-toast-enter');toast.classList.add('fix-toast-exit');},durationMs);}
function validateGuestFilename(raw){let name=String(raw||'').trim();name=name.replace(/[^a-zA-Z0-9._-]/g,'');if(!name)return null;if(!name.includes('.'))name+='.cpp';if(name==='.cpp')return null;return name;}
function showSaveGuestDraftModal(snapshot){return new Promise((resolve)=>{const existingFiles=Array.isArray(snapshot?.files)?snapshot.files:[];let maxN=0;for(const f of existingFiles){const nm=f?.file_name||f?.filename||'';const m=/^untitled-(\d+)\.cpp$/i.exec(nm);if(m){const v=Number(m[1]);if(v>maxN)maxN=v;}}
const suggestedName=`untitled-${maxN + 1}.cpp`;const overlay=document.createElement('div');overlay.id='goc-draft-modal-overlay';overlay.style.cssText=['position:fixed','inset:0','z-index:99999','display:flex','align-items:center','justify-content:center','background:rgba(0,0,0,0.65)','backdrop-filter:blur(4px)','animation:gocFadeIn 0.18s ease'].join(';');overlay.innerHTML=`
<style>
@keyframes gocFadeIn  { from { opacity:0 } to { opacity:1 } }
@keyframes gocSlideUp { from { opacity:0; transform:translateY(18px) } to { opacity:1; transform:translateY(0) } }
#goc-draft-modal {
    background: var(--vscode-sidebar);
    border: 1px solid var(--border-color);
    border-radius: 14px;
    padding: 28px 28px 24px;
    max-width: 420px;
    width: calc(100vw - 40px);
    box-shadow: 0 24px 64px rgba(0,0,0,0.5);
    animation: gocSlideUp 0.22s ease;
    display: flex;
    flex-direction: column;
    gap: 20px;
}
#goc-draft-modal h2 {
    font-size: 1rem;
    font-weight: 700;
    color: var(--text-primary);
    display: flex;
    align-items: center;
    gap: 10px;
    margin: 0;
}
#goc-draft-modal h2 svg { flex-shrink:0; color:var(--primary); }
#goc-draft-modal p {
    font-size: 0.8375rem;
    color: var(--text-secondary);
    line-height: 1.55;
    margin: 0;
}
#goc-draft-modal label {
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--text-secondary);
    display: block;
    margin-bottom: 7px;
}
#goc-draft-filename {
    width: 100%;
    background: var(--vscode-bg);
    border: 1px solid var(--border-color);
    border-radius: 7px;
    color: var(--text-primary);
    font-size: 0.875rem;
    padding: 9px 12px;
    outline: none;
    transition: border-color 0.15s ease;
    font-family: 'JetBrains Mono', monospace;
}
#goc-draft-filename:focus { border-color: var(--primary); }
#goc-draft-filename.invalid { border-color: var(--danger); }
#goc-filename-error {
    font-size: 0.775rem;
    color: var(--danger);
    min-height: 16px;
    display: block;
    margin-top: 4px;
}
#goc-draft-modal .goc-modal-actions {
    display: flex;
    gap: 10px;
    justify-content: flex-end;
    flex-wrap: wrap;
}
#goc-discard-btn {
    padding: 8px 16px;
    border-radius: 7px;
    border: 1px solid var(--border-color);
    background: transparent;
    color: var(--text-secondary);
    font-size: 0.8125rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.15s ease;
}
#goc-discard-btn:hover { background: rgba(255,68,68,0.1); border-color: var(--danger); color: var(--danger); }
#goc-save-btn {
    padding: 8px 20px;
    border-radius: 7px;
    border: none;
    background: var(--primary);
    color: var(--primary-text);
    font-size: 0.8125rem;
    font-weight: 700;
    cursor: pointer;
    transition: background 0.15s ease, transform 0.1s ease;
}
#goc-save-btn:hover:not(:disabled) { background: var(--primary-hover); }
#goc-save-btn:active:not(:disabled) { transform: scale(0.97); }
#goc-save-btn:disabled { opacity: 0.45; cursor: not-allowed; }
</style>
<div id="goc-draft-modal" role="dialog" aria-modal="true" aria-labelledby="goc-modal-title">
    <h2 id="goc-modal-title">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4z"/>
            <polyline points="17 3 17 8 7 8"/>
            <line x1="12" y1="18" x2="12" y2="12"/>
            <line x1="9" y1="15" x2="15" y2="15"/>
        </svg>
        Save your guest code?
    </h2>
    <p>You have code that isn't saved to your cloud account yet. Give it a name to keep it, or discard it and start fresh.</p>
    <div>
        <label for="goc-draft-filename">File name</label>
        <input
            type="text"
            id="goc-draft-filename"
            autocomplete="off"
            spellcheck="false"
            value="${suggestedName}"
            placeholder="my-project.cpp"
        />
        <span id="goc-filename-error" aria-live="polite"></span>
    </div>
    <div class="goc-modal-actions">
        <button id="goc-discard-btn" type="button">Discard</button>
        <button id="goc-save-btn" type="button">Save to Cloud</button>
    </div>
</div>`;document.body.appendChild(overlay);const input=overlay.querySelector('#goc-draft-filename');const errorEl=overlay.querySelector('#goc-filename-error');const saveBtn=overlay.querySelector('#goc-save-btn');const discardBtn=overlay.querySelector('#goc-discard-btn');requestAnimationFrame(()=>{input.focus();input.select();});function setError(msg){errorEl.textContent=msg||'';input.classList.toggle('invalid',Boolean(msg));saveBtn.disabled=Boolean(msg);}
function validateInput(){const clean=validateGuestFilename(input.value);if(!clean){setError('Invalid filename. Use letters, numbers, hyphens and underscores.');return null;}
setError('');return clean;}
input.addEventListener('input',validateInput);function cleanup(){overlay.remove();}
function onSave(){const clean=validateInput();if(!clean)return;cleanup();resolve({action:'save',filename:clean});}
function onDiscard(){cleanup();resolve({action:'discard'});}
saveBtn.addEventListener('click',onSave);discardBtn.addEventListener('click',onDiscard);function onKey(e){if(e.key==='Escape'){onDiscard();document.removeEventListener('keydown',onKey);}
if(e.key==='Enter'){onSave();document.removeEventListener('keydown',onKey);}}
document.addEventListener('keydown',onKey);overlay.addEventListener('click',(e)=>{if(e.target===overlay)onDiscard();});});}
async function handleGoogleCredentialResponse(credentialResponse){const idToken=credentialResponse?.credential;if(!idToken)throw new Error('Google did not return an ID token');const{response,payload}=await fetchJson('/api/auth/google',{method:'POST',body:JSON.stringify({id_token:idToken}),});if(!response.ok)throw new Error(payload?.error||'Sign in failed');const user=normalizeSessionUser(payload)||{email:payload?.email||'',name:payload?.display_name||'User',image:'',};safeUpdateLoginUI(true,userForUi(user));setAuthStatus(`Signed in as ${user.email}`);setExplorerLoading(true,'Syncing...');showProgress();try{const editorContent=editor?editor.getValue():'';const editorHash=await computeSha256(editorContent);let snapshot=await fetchCloudSnapshot();updateCloudStateFromPayload(snapshot);renderFileExplorer();let openTarget=null;if(DEMO_HASHES.has(editorHash)){Logger.info('[SignIn] Editor contains demo/template code — skipping save');openTarget=resolveDefaultOpenTarget(snapshot);}else if(CLOUD_STATE.hashToFileKey.has(editorHash)){const matchKey=CLOUD_STATE.hashToFileKey.get(editorHash);const matchFile=CLOUD_STATE.files.get(matchKey);if(matchFile){Logger.info(`[SignIn] Guest code matches cloud file "${matchFile.filename}" — opening directly`);openTarget={folder:matchFile.folder_key,filename:matchFile.filename};setTimeout(()=>showToast(`Opened existing file: ${matchFile.filename}`),600);}else{openTarget=resolveDefaultOpenTarget(snapshot);}}else{setExplorerLoading(false);hideProgress();let modalResult;try{modalResult=await showSaveGuestDraftModal(snapshot);}catch{modalResult={action:'discard'};}
setExplorerLoading(true,'Syncing...');showProgress();if(modalResult.action==='save'){Logger.info(`[SignIn] User chose to save guest code as "${modalResult.filename}"`);const saved=await saveGuestCodeWithName(snapshot,editorContent,modalResult.filename);openTarget={folder:folderKey(saved.folderId),filename:saved.fileName};updateCloudStateFromPayload(snapshot);renderFileExplorer();}else{Logger.info('[SignIn] User discarded guest code — restoring default template');if(editor){editor.setValue(DEFAULT_SOURCE);editor.clearSelection();}
openTarget=resolveDefaultOpenTarget(snapshot);}}
await FileDB.clear().catch(()=>{});Promise.all(Array.from(CLOUD_STATE.files.entries()).map(([key,file])=>FileDB.put({id:key,name:file.filename,content:file.content||'',lastSavedHash:file.content_hash||null,lastModified:Date.now(),dirty:false,folderId:file.folder_id,folderKey:file.folder_key}).catch(()=>{}))).catch(()=>{});if(openTarget)await openFile(openTarget.folder,openTarget.filename,{skipSave:true});}finally{setExplorerLoading(false);hideProgress();}}
function waitForGoogleIdentityScript(){if(window.google?.accounts?.id)return Promise.resolve();return new Promise((resolve,reject)=>{let attempts=0;const interval=setInterval(()=>{if(window.google?.accounts?.id){clearInterval(interval);resolve();return;}
attempts+=1;if(attempts>=100){clearInterval(interval);reject(new Error('Google Identity Services failed to load'));}},100);});}
async function initGoogleIdentity(clientId){if(!clientId)return;if(googleIdentityInitPromise)return googleIdentityInitPromise;googleIdentityInitPromise=(async()=>{await waitForGoogleIdentityScript();window.google.accounts.id.initialize({client_id:clientId,callback:(response)=>{handleGoogleCredentialResponse(response).catch((error)=>{Logger.error(`[Auth] ${error.message}`);setAuthStatus('Sign in failed. Try again.');alert(`Sign in failed: ${error.message}`);});},auto_select:false,cancel_on_tap_outside:true,});const renderTarget=document.getElementById('google-btn-render');if(renderTarget){window.google.accounts.id.renderButton(renderTarget,{theme:'outline',size:'large',shape:'rectangular',text:'signin_with',width:196});}
googleIdentityReady=true;})();return googleIdentityInitPromise;}
async function signInWithGoogle(){try{const config=await loadAuthConfig();if(!config.authEnabled){alert('Authentication is not configured on the server.');return;}
await initGoogleIdentity(config.googleClientId);if(!googleIdentityReady||!window.google?.accounts?.id)throw new Error('Google sign-in is unavailable');setAuthStatus('Waiting for Google sign-in...');window.google.accounts.id.prompt((notification)=>{if(notification.isNotDisplayed()||notification.isSkippedMoment()){Logger.warn('[Auth] One Tap suppressed, using rendered button instead');}});}catch(error){Logger.error(`[Auth] ${error.message}`);setAuthStatus('Sign in failed. Try again.');alert(`Sign in failed: ${error.message}`);}}
async function signOut(){const currentCode=editor?editor.getValue():null;if(isUserLoggedIn&&DIRTY_FLAG.isDirty){try{await forceSaveActiveFile('signOut',{silent:true});}catch{}}
safeUpdateLoginUI(false);setAuthStatus('Unlimited projects · Access anywhere');if(currentCode!==null){SAVE_STATE.lastSavedHash=await computeSha256(currentCode);DIRTY_FLAG.isDirty=false;updateSaveIndicator();}
(async()=>{try{await fetch('/api/auth/logout',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:'{}'});}catch{}
await clearAllLocalDrafts().catch(()=>{});if(currentCode!==null){await setLocalDraft('root','main.cpp',currentCode).catch(()=>{});}})();}
function updateCloudStateFromPayload(payload){setDefaultFolderState();CLOUD_STATE.lastOpenedFileId=payload?.last_opened_file_id||payload?.data?.last_opened_file_id||null;CLOUD_STATE.files.clear();const folders=Array.isArray(payload?.folders)?payload.folders:(Array.isArray(payload?.data?.folders)?payload.data.folders:[]);const files=Array.isArray(payload?.files)?payload.files:(Array.isArray(payload?.data?.files)?payload.data.files:[]);folders.forEach((folder)=>{if(!folder?.id||!folder?.folder_name)return;CLOUD_STATE.folders.add(folder.id);CLOUD_STATE.folderIdToName.set(folder.id,folder.folder_name);CLOUD_STATE.folderNameToId.set(folder.folder_name,folder.id);});CLOUD_STATE.hashToFileKey=CLOUD_STATE.hashToFileKey||new Map();CLOUD_STATE.hashToFileKey.clear();files.forEach((file)=>{const name=file.file_name||file.filename;if(!name)return;const id=file.folder_id||file.folderId||null;const key=folderKey(id);const content=file.file_content??file.content??'';const hash=file.content_hash||null;const fk=fileKey(key,name);CLOUD_STATE.files.set(fk,{id:file.id||file.file_id||fk,filename:name,folder_id:id,folder_key:key,folder_name:file.folder_name||file.folderName||getFolderName(id),content,file_size:file.file_size??computeBytes(content),content_hash:hash});if(hash)CLOUD_STATE.hashToFileKey.set(hash,fk);});if(!CLOUD_STATE.folderIdToName.has(CLOUD_STATE.selectedFolderKey)){const firstFolder=Array.from(CLOUD_STATE.folderIdToName.keys())[0]||ROOT_FOLDER_KEY;CLOUD_STATE.selectedFolderKey=firstFolder;}}
async function refreshCloudFiles(force=false){if(!isUserLoggedIn)return;const lastRefresh=CLOUD_STATE.lastRefresh||0;if(!force&&Date.now()-lastRefresh<2000){renderFileExplorer();return;}
CLOUD_STATE.lastRefresh=Date.now();showProgress();setExplorerLoading(true,'Loading files...');try{const{response,payload}=await fetchJson('/api/files',{method:'GET'});if(response.status===401){safeUpdateLoginUI(false);setAuthStatus('Session expired. Please sign in again.');return;}
if(!response.ok)throw new Error(payload?.error||'Failed to load files');updateCloudStateFromPayload(payload);renderFileExplorer();}catch(error){Logger.warn(`[Files] ${error.message}`);alert(`Unable to load cloud files: ${error.message}`);}finally{setExplorerLoading(false);hideProgress();}}
async function openFile(folder,filename,options={}){if(!options.skipSave)await forceSaveActiveFile('fileSwitch',{silent:true});const key=fileKey(folder,filename);CLOUD_STATE.activeFileKey=key;setSelectedFolder(folder);let content=CLOUD_STATE.files.get(key)?.content??null;if(content===null)content=await getLocalDraft(folder,filename);if(content===null)content='';if(editor){editor.setValue(content);editor.clearSelection();}
SAVE_STATE.lastSavedHash=await computeSha256(content);SAVE_STATE.lastSaveTime=Date.now();DIRTY_FLAG.isDirty=false;updateSaveIndicator();highlightActiveFile();if(!isUserLoggedIn)saveLastOpenedFile(key);if(isUserLoggedIn&&!options.skipSave){const opened=CLOUD_STATE.files.get(key);if(opened?.id&&opened.id!==CLOUD_STATE.lastOpenedFileId){fetchJson('/api/file/save',{method:'POST',body:JSON.stringify({folder_id:opened.folder_id??null,file_name:opened.filename,content})}).then(({response,payload})=>{if(response.ok){CLOUD_STATE.lastOpenedFileId=payload?.file_id||opened.id;}}).catch(()=>{});}}}
async function persistLocalSave(code){const info=activeFileInfo();const hash=await computeSha256(code);await FileDB.put({id:info.key,name:info.filename,content:code,lastSavedHash:hash,lastModified:Date.now(),dirty:false,folderId:info.folderId,folderKey:info.folder});SAVE_STATE.lastSavedHash=hash;SAVE_STATE.lastSaveTime=Date.now();DIRTY_FLAG.isDirty=false;updateSaveIndicator();}
async function forceSaveActiveFile(trigger='manual',options={}){const silent=options.silent===true;if(!editor||isSaving)return{skipped:true};const skipProgress=trigger==='compileRun';if(!skipProgress)showProgress();try{const code=editor.getValue();if(!isUserLoggedIn){await persistLocalSave(code);if(!silent)Logger.success(`[Save] Saved (${trigger})`);return{success:true,local:true};}
let info=activeFileInfo();if(!info.folderId&&CLOUD_STATE.folderIdToName.size===0){const mainFolderId=await ensureMainFolder();const mainFolderKey=folderKey(mainFolderId);const key=fileKey(mainFolderKey,info.filename);info={...info,folder:mainFolderKey,folderId:mainFolderId,key};CLOUD_STATE.activeFileKey=key;setSelectedFolder(mainFolderKey);}
const hash=await computeSha256(code);if(SAVE_STATE.lastSavedHash===hash){DIRTY_FLAG.isDirty=false;updateSaveIndicator();if(!silent)Logger.info(`[Save] Skipped (${trigger}) - unchanged`);return{skipped:true,unchanged:true};}
isSaving=true;SAVE_STATE.pendingHash=hash;try{const{response,payload}=await fetchJson('/api/file/save',{method:'POST',body:JSON.stringify({folder_id:info.folderId,file_name:info.filename,content:code})});if(response.status===401){safeUpdateLoginUI(false);setAuthStatus('Session expired. Please sign in again.');throw new Error('Session expired');}
if(!response.ok)throw new Error(payload?.error||'Failed to save file');CLOUD_STATE.files.set(info.key,{id:payload?.file_id||info.key,filename:info.filename,folder_id:info.folderId,folder_key:info.folder,folder_name:getFolderName(info.folderId),content:code,file_size:payload?.file_size??computeBytes(code),content_hash:payload?.content_hash||hash});const confirmedHash=payload?.content_hash||hash;FileDB.put({id:info.key,name:info.filename,content:code,lastSavedHash:confirmedHash,lastModified:Date.now(),dirty:false,folderId:info.folderId,folderKey:info.folder}).catch(()=>{});localStorage.setItem('tc_code',code);SAVE_STATE.lastSavedHash=confirmedHash;SAVE_STATE.lastSaveTime=Date.now();DIRTY_FLAG.isDirty=false;updateSaveIndicator();renderFileExplorer();if(!silent)Logger.success(`[Save] Saved (${trigger})`);return{success:true,changed:payload?.changed!==false};}finally{isSaving=false;SAVE_STATE.pendingHash=null;}}finally{if(!skipProgress)hideProgress();}}
async function saveCode(){const saveBtn=document.getElementById('save-btn');const btnText=saveBtn?.querySelector('.btn-text');const originalText=btnText?.textContent||'Save';if(btnText)btnText.textContent='Saving...';try{await forceSaveActiveFile('manual',{silent:false});if(btnText)btnText.textContent='Saved';}
catch(error){if(btnText)btnText.textContent='Error';Logger.error(`[Save] ${error.message}`);}
finally{setTimeout(()=>{if(btnText)btnText.textContent=originalText;},1200);}}
async function createNewFolder(folderName){if(!isUserLoggedIn)return alert('Sign in to create cloud folders.');const cleanName=(folderName||'').trim();if(!cleanName)return;showProgress();try{const{response,payload}=await fetchJson('/api/folder/create',{method:'POST',body:JSON.stringify({folder_name:cleanName})});if(!response.ok)throw new Error(payload?.error||'Failed to create folder');CLOUD_STATE.folderIdToName.set(payload.id,payload.folder_name);CLOUD_STATE.folderNameToId.set(payload.folder_name,payload.id);CLOUD_STATE.folders.add(payload.id);setSelectedFolder(payload.id);renderFileExplorer();return payload.id;}finally{hideProgress();}}
async function createNewFile(filename){if(!isUserLoggedIn)return alert('Sign in to create cloud files.');showProgress();try{let cleanName=(filename||'').trim();if(!cleanName)return;if(!cleanName.includes('.'))cleanName+='.cpp';cleanName=cleanName.replace(/[^a-zA-Z0-9._-]/g,'');if(!cleanName||cleanName==='.cpp')return alert('Invalid file name');let targetFolderId=folderId(CLOUD_STATE.selectedFolderKey)||activeFileInfo().folderId;if(!targetFolderId)targetFolderId=Array.from(CLOUD_STATE.folderIdToName.keys())[0]||null;if(!targetFolderId){targetFolderId=await ensureMainFolder();setSelectedFolder(folderKey(targetFolderId));}
if(remoteFileByName(targetFolderId,cleanName))return alert(`File "${cleanName}" already exists`);const starter=buildStarterSource(cleanName);const{response:saveResponse,payload:savePayload}=await fetchJson('/api/file/save',{method:'POST',body:JSON.stringify({folder_id:targetFolderId,file_name:cleanName,content:starter})});if(!saveResponse.ok){if(saveResponse.status===409){await refreshCloudFiles(true);const existing=remoteFileByName(targetFolderId,cleanName);if(existing){await openFile(existing.folder_key,existing.filename,{skipSave:true});return;}}
throw new Error(savePayload?.error||'Failed to save starter code');}
const key=folderKey(targetFolderId);CLOUD_STATE.files.set(fileKey(key,cleanName),{id:savePayload?.file_id||fileKey(key,cleanName),filename:cleanName,folder_id:targetFolderId||null,folder_key:key,folder_name:getFolderName(targetFolderId),content:starter,file_size:savePayload?.file_size??computeBytes(starter),content_hash:savePayload?.content_hash||null});await setLocalDraft(key,cleanName,starter);renderFileExplorer();await openFile(key,cleanName,{skipSave:true});}finally{hideProgress();}}
async function deleteFolder(folder,name){const id=folderId(folder);if(!id)return;if(!confirm(`Delete folder "${name}" and all files inside it?\nThis cannot be undone.`))return;showProgress();try{const deletingActiveFolder=CLOUD_STATE.files.get(CLOUD_STATE.activeFileKey||'')?.folder_id===id;const{response,payload}=await fetchJson('/api/folder/delete',{method:'DELETE',body:JSON.stringify({folder_id:id})});if(!response.ok)throw new Error(payload?.error||'Failed to delete folder');for(const[key,file]of CLOUD_STATE.files.entries())if(file.folder_id===id){CLOUD_STATE.files.delete(key);await clearLocalDraft(file.folder_key,file.filename);}
CLOUD_STATE.folderIdToName.delete(id);CLOUD_STATE.folders.delete(id);setSelectedFolder(Array.from(CLOUD_STATE.folderIdToName.keys())[0]||ROOT_FOLDER_KEY);renderFileExplorer();if(deletingActiveFolder){const nextFile=getFirstVisibleCloudFile();if(nextFile)await openFile(nextFile.folder_key,nextFile.filename,{skipSave:true});}}finally{hideProgress();}}
async function deleteFile(folder,filename){if(filename==='main.cpp'){alert("main.cpp is the primary file and cannot be deleted.");return;}
if(!confirm(`Delete "${filename}"?\nThis cannot be undone.`))return;showProgress();try{const key=fileKey(folder,filename);const file=CLOUD_STATE.files.get(key);if(!file)return;const{response,payload}=await fetchJson('/api/file/delete',{method:'DELETE',body:JSON.stringify({file_id:file.id})});if(!response.ok)throw new Error(payload?.error||'Failed to delete file');CLOUD_STATE.files.delete(key);await clearLocalDraft(folder,filename);renderFileExplorer();if(CLOUD_STATE.activeFileKey===key){const nextFile=getFirstVisibleCloudFile();if(nextFile)await openFile(nextFile.folder_key,nextFile.filename,{skipSave:true});else if(editor){editor.setValue(DEFAULT_SOURCE);DIRTY_FLAG.isDirty=false;updateSaveIndicator();}}}finally{hideProgress();}}
const LAST_OPENED_FILE_KEY='compiler_last_opened_v1';function saveLastOpenedFile(key){if(key)localStorage.setItem(LAST_OPENED_FILE_KEY,key);}
function getLastOpenedFile(){return localStorage.getItem(LAST_OPENED_FILE_KEY);}
async function downloadFile(folder,filename){const key=fileKey(folder,filename);let content;if(key===CLOUD_STATE.activeFileKey&&editor){content=editor.getValue();}else{content=CLOUD_STATE.files.get(key)?.content??await getLocalDraft(folder,filename)??'';}
const blob=new Blob([content],{type:'text/plain'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();document.body.removeChild(a);URL.revokeObjectURL(url);}
function scheduleAutosave(){if(CLOUD_STATE.autosaveTimer)clearTimeout(CLOUD_STATE.autosaveTimer);CLOUD_STATE.autosaveTimer=setTimeout(async()=>{CLOUD_STATE.autosaveTimer=null;if(!DIRTY_FLAG.isDirty)return;if(window.__aiPreviewPending===true)return;try{if(isUserLoggedIn){await forceSaveActiveFile('idle',{silent:true});}else{if(editor)await persistLocalSave(editor.getValue());}}catch(error){Logger.warn(`[Autosave] ${error.message}`);}},AUTOSAVE_DELAY_MS);}
async function initAuth(){try{const config=await loadAuthConfig();if(!config.storageEnabled)setAuthStatus('Cloud storage is not configured');if(config.authEnabled&&config.googleClientId){initGoogleIdentity(config.googleClientId).catch((error)=>{Logger.warn(`[Auth] ${error.message}`);setAuthStatus('Google sign-in is unavailable');});}else{const authSection=document.getElementById('auth-section');if(authSection){authSection.innerHTML=`
                    <div style="background: var(--bg-modifier-hover); border: 1px solid var(--border-color); padding: 12px; border-radius: 6px; text-align: center; margin-top: 10px;">
                        <div style="color: var(--text-muted); font-size: 13px; line-height: 1.4;">Auth client is not configured. Files will be saved locally in your browser.</div>
                    </div>
                `;}
setAuthStatus('');}
const loggedIn=await checkSession();if(loggedIn){await refreshCloudFiles(true);const active=getCloudFileById(CLOUD_STATE.lastOpenedFileId)||getMainCppFile()||getFirstCloudFile();if(active)await openFile(active.folder_key,active.filename,{skipSave:true});}}catch(error){Logger.warn(`[Auth] ${error.message}`);safeUpdateLoginUI(false);setAuthStatus('Unlimited projects · Access anywhere');}}
const googleSigninBtn=document.getElementById('google-signin-btn');if(googleSigninBtn)googleSigninBtn.addEventListener('click',signInWithGoogle);const storageNewFolderBtn=document.getElementById('new-folder-btn');if(storageNewFolderBtn)storageNewFolderBtn.addEventListener('click',async()=>{const name=prompt('Enter a folder name:');if(!name||!name.trim())return;try{await createNewFolder(name.trim());}catch(error){Logger.warn(`[Folder] ${error.message}`);alert(error.message);}});function startAuthInit(){initAuth().catch((error)=>{Logger.warn(`[Auth] ${error.message}`);safeUpdateLoginUI(false);setAuthStatus('Unlimited projects · Access anywhere');});}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',startAuthInit,{once:true});}else{startAuthInit();};let functionsMap={};let constantsMap={};let headerFiles=[];let dataLoaded=false;let startAutocomplete=null;let pendingIncludeHeaderSuggestions=false;let cachedFunctionOptions=null;let applySnippet=null;let autocompleteCacheField=null;const constantCache=new Map();const INCLUDE_DIRECTIVE='#include';const DEFAULT_INCLUDE_HEADERS=['graphics.h','conio.h','dos.h'];const BOILERPLATE_LABEL='boilerplate';const BOILERPLATE_TEMPLATE=`#include <graphics.h>
#include <conio.h>

int main() {
    int gd = DETECT, gm;
    initgraph(&gd, &gm, "");

    getch();
    closegraph();
    return 0;
}`;const SNIPPETS={for:{label:'for',detail:'counting loop',type:'keyword',boost:5,apply:createSnippetApply('for (int ${i} = 0; ${i} < ${n}; ${i}++) {\n\t${}\n}')},while:{label:'while',detail:'while loop',type:'keyword',boost:5,apply:createSnippetApply('while (${condition}) {\n\t${}\n}')},if:{label:'if',detail:'conditional block',type:'keyword',boost:5,apply:createSnippetApply('if (${condition}) {\n\t${}\n}')},main:{label:'main',detail:'main function',type:'keyword',boost:5,apply:createSnippetApply('int main() {\n\t${}\n\treturn 0;\n}')},anim:{label:'anim',detail:'animation loop',type:'keyword',boost:5,apply:createSnippetApply('while (!kbhit()) {\n\tcleardevice();\n\t${}\n\tdelay(16);\n}')}};function createSnippetApply(template){return(view,completion,from,to)=>{if(typeof applySnippet==='function'){applySnippet(template)(view,completion,from,to);return;}
const plainText=template.replace(/\$\{([^}]*)\}/g,'$1');view.dispatch({changes:{from,to,insert:plainText},selection:{anchor:from+plainText.length}});};}
function resetAutocompleteCaches(){cachedFunctionOptions=null;constantCache.clear();}
function extractVariablesFromLine(lineText){const variables=[];const variablePattern=/\b(int|float|double|char\s*\*?)\s+(\w+)/g;let match;while((match=variablePattern.exec(lineText))!==null){variables.push(match[2]);}
return variables;}
function createLineVariableEntry(lineText){return{text:lineText,variables:extractVariablesFromLine(lineText)};}
function buildVariableListFromLineEntries(lineEntries){const variables=[];const seen=new Set();for(const entry of lineEntries){for(const variableName of entry.variables){if(!seen.has(variableName)){seen.add(variableName);variables.push(variableName);}}}
return variables;}
function buildAutocompleteDocCache(doc){const source=doc.toString();const lineEntries=[];for(let lineNumber=1;lineNumber<=doc.lines;lineNumber++){lineEntries.push(createLineVariableEntry(doc.line(lineNumber).text));}
return{source,lineEntries,variables:buildVariableListFromLineEntries(lineEntries),includedHeaders:getIncludedHeaders(source)};}
function getChangedLineRange(doc,from,to){const safeFrom=Math.min(from,doc.length);const safeEnd=Math.min(to>from?to-1:from,doc.length);return{from:doc.lineAt(safeFrom).number,to:doc.lineAt(safeEnd).number};}
function updateAutocompleteDocCache(previousCache,transaction){const source=transaction.state.doc.toString();const lineEntries=previousCache.lineEntries.slice();const replacements=[];transaction.changes.iterChangedRanges((fromA,toA,fromB,toB)=>{const oldRange=getChangedLineRange(transaction.startState.doc,fromA,toA);const newRange=getChangedLineRange(transaction.state.doc,fromB,toB);const newEntries=[];for(let lineNumber=newRange.from;lineNumber<=newRange.to;lineNumber++){newEntries.push(createLineVariableEntry(transaction.state.doc.line(lineNumber).text));}
replacements.push({oldFromLine:oldRange.from,oldToLine:oldRange.to,newEntries});});let lineOffset=0;for(const replacement of replacements){const startIndex=replacement.oldFromLine-1+lineOffset;const deleteCount=replacement.oldToLine-replacement.oldFromLine+1;lineEntries.splice(startIndex,deleteCount,...replacement.newEntries);lineOffset+=replacement.newEntries.length-deleteCount;}
if(lineEntries.length!==transaction.state.doc.lines){return buildAutocompleteDocCache(transaction.state.doc);}
return{source,lineEntries,variables:buildVariableListFromLineEntries(lineEntries),includedHeaders:getIncludedHeaders(source)};}
function getAutocompleteDocCache(state){if(autocompleteCacheField){const cached=state.field(autocompleteCacheField,false);if(cached){return cached;}}
return buildAutocompleteDocCache(state.doc);}
async function loadData(){if(dataLoaded)return;try{const response=await fetch('/static/assets/functions.2.json');if(!response.ok)throw new Error('Failed to load functions data');const data=await response.json();functionsMap={};constantsMap={};headerFiles=[];resetAutocompleteCaches();for(const func of data.functions){functionsMap[func.name]=func;}
if(data.constants){for(const[key,values]of Object.entries(data.constants)){constantsMap[key]=values;}}
if(Array.isArray(data.headers)){headerFiles=data.headers.map((header)=>String(header||'').trim().toLowerCase()).filter(Boolean);}
dataLoaded=true;}catch(error){dataLoaded=false;resetAutocompleteCaches();console.error('Autocomplete: failed to load data',error);}}
loadData();function getIncludedHeaders(sourceOrState){if(sourceOrState&&typeof sourceOrState!=='string'){const cached=getAutocompleteDocCache(sourceOrState);if(cached?.includedHeaders){return cached.includedHeaders;}}
const includedHeaders=new Set();const includePattern=/^\s*#include\s*[<"]([^>"\r\n]+)[>"]/gm;const source=typeof sourceOrState==='string'?sourceOrState:sourceOrState?.doc?.toString?.()??'';let match;while((match=includePattern.exec(source))!==null){includedHeaders.add(match[1].trim().toLowerCase());}
return includedHeaders;}
function buildHeaderOptions(headers,preserveOrder=false,closeDelimiter='>'){const totalHeaders=headers.length;return headers.map((header,index)=>({label:header,type:'text',boost:preserveOrder?(totalHeaders-index)*100:undefined,apply:(view,completion,from,to)=>{const hasClosingDelimiter=view.state.sliceDoc(to,to+1)===closeDelimiter;const insertText=completion.label+(hasClosingDelimiter?'':closeDelimiter);view.dispatch({changes:{from,to,insert:insertText},selection:{anchor:from+completion.label.length+(hasClosingDelimiter?1:closeDelimiter.length)}});pendingIncludeHeaderSuggestions=false;}}));}
function getHeaderSuggestions(source,prefix,useStarterList){const normalizedPrefix=(prefix||'').toLowerCase();if(useStarterList){const includedHeaders=getIncludedHeaders(source);return DEFAULT_INCLUDE_HEADERS.filter((header)=>headerFiles.includes(header)).filter((header)=>!includedHeaders.has(header));}
return headerFiles.filter((header)=>header.startsWith(normalizedPrefix));}
function isBoilerplatePrefix(prefix){if(!prefix)return false;const normalizedPrefix=prefix.toLowerCase();return normalizedPrefix.length>=1&&BOILERPLATE_LABEL.startsWith(normalizedPrefix);}
function isEffectivelyEmpty(source,typedWord){const normalized=source.split('\n').filter((line)=>{const trimmed=line.trim();return trimmed&&!trimmed.startsWith('//');}).join('').trim();return normalized.toLowerCase()===(typedWord||'').toLowerCase();}
function getBoilerplateTrigger(source,word){if(!word||!word.text){return null;}
if(!source.trim()||!isEffectivelyEmpty(source,word.text)){return null;}
if(!isBoilerplatePrefix(word.text)){return null;}
return{from:word.from,to:word.to,text:word.text};}
function isInsideString(textUpToCursor){const quoteCount=(textUpToCursor.match(/"/g)||[]).length;return quoteCount%2===1;}
function isLineComment(textUpToCursor){return textUpToCursor.trimStart().startsWith('//');}
function isInsideBlockComment(source,pos){const sourceUpToCursor=source.slice(0,pos);const openCount=(sourceUpToCursor.match(/\/\*/g)||[]).length;const closeCount=(sourceUpToCursor.match(/\*\//g)||[]).length;return openCount>closeCount;}
function detectIncludeContext(textUpToCursor,pos){const includeKeywordMatch=textUpToCursor.match(/#in\w*$/);if(includeKeywordMatch){return{type:'include-keyword',prefix:includeKeywordMatch[0],from:pos-includeKeywordMatch[0].length};}
const includeHeaderMatch=textUpToCursor.match(/^\s*#include\s*[<"]([^>"'\r\n]*)$/);if(includeHeaderMatch){const prefix=includeHeaderMatch[1]||'';const openDelimiter=textUpToCursor[textUpToCursor.length-prefix.length-1]||'<';return{type:'headers',prefix,from:pos-prefix.length,openDelimiter,closeDelimiter:openDelimiter==='"'?'"':'>'};}
return null;}
function detectParenContext(textUpToCursor){let counter=0;let openParenIndex=-1;for(let i=textUpToCursor.length-1;i>=0;i--){const char=textUpToCursor[i];if(char===')'){counter++;}else if(char==='('){counter--;if(counter<0){openParenIndex=i;break;}}}
if(openParenIndex===-1){const match=textUpToCursor.match(/[\w]+$/);const prefix=match?match[0]:'';return{type:'functions',prefix};}
const textBeforeParen=textUpToCursor.slice(0,openParenIndex);const funcMatch=textBeforeParen.match(/[\w]+$/);const funcName=funcMatch?funcMatch[0]:'';let commaCount=0;let innerCounter=0;const innerText=textUpToCursor.slice(openParenIndex+1);for(let i=0;i<innerText.length;i++){const char=innerText[i];if(char==='('){innerCounter++;}else if(char===')'){innerCounter--;}else if(char===','&&innerCounter===0){commaCount++;}}
const prefixMatch=innerText.match(/[\w]+$/);const prefix=prefixMatch?prefixMatch[0]:'';return{type:'constants',funcName,paramIndex:commaCount.toString(),prefix};}
function extractVariables(source){if(typeof source==='string'){const lineEntries=source.split('\n').map((lineText)=>createLineVariableEntry(lineText));return buildVariableListFromLineEntries(lineEntries);}
return getAutocompleteDocCache(source).variables;}
function fuzzyMatch(str,pattern){let patternIndex=0;for(let i=0;i<str.length&&patternIndex<pattern.length;i++){if(str[i]===pattern[patternIndex]){patternIndex++;}}
return patternIndex===pattern.length;}
function buildFunctionOptions(){if(cachedFunctionOptions){return cachedFunctionOptions;}
cachedFunctionOptions=Object.entries(functionsMap).map(([name,func])=>({label:name,detail:'()',type:'function',boost:func.boost??0,info:()=>{const d=buildTooltipHTML(func.info);return{dom:d};},apply:(view,completion,from,to)=>{const isInside=func.cursor==='inside';const insertText=name+'()';const activeParamIndex=isInside?0:null;view.dispatch({changes:{from,to,insert:insertText},selection:{anchor:from+name.length+(isInside?1:2)}});view.dispatch({effects:showSelectionTooltip.of({name,activeParamIndex})});}}));return cachedFunctionOptions;}
function detectContext(state,pos,source){const line=state.doc.lineAt(pos);const textUpToCursor=line.text.slice(0,pos-line.from);if(isLineComment(textUpToCursor)){return{type:'comment'};}
if(isInsideBlockComment(source,pos)){return{type:'comment'};}
if(isInsideString(textUpToCursor)){return{type:'string'};}
const includeContext=detectIncludeContext(textUpToCursor,pos);if(includeContext){return includeContext;}
const preprocessorMatch=textUpToCursor.match(/#\w*$/);if(preprocessorMatch){return{type:'preprocessor',prefix:preprocessorMatch[0],from:pos-preprocessorMatch[0].length};}
return detectParenContext(textUpToCursor,pos);}
function getCompletions(context){const state=context.state;const pos=context.pos;const docCache=getAutocompleteDocCache(state);const source=docCache.source;const currentWord=context.matchBefore(/\w*/);const boilerplateTrigger=getBoilerplateTrigger(source,currentWord);if(boilerplateTrigger&&pos>=boilerplateTrigger.from&&pos<=boilerplateTrigger.to){return{from:boilerplateTrigger.from,options:[{label:BOILERPLATE_LABEL,detail:'graphics.h template',type:'keyword',boost:1000,apply:(view)=>{view.dispatch({changes:{from:0,to:view.state.doc.length,insert:BOILERPLATE_TEMPLATE},selection:{anchor:BOILERPLATE_TEMPLATE.length}});}}],validFor:/^\w*$/i};}
if(!dataLoaded)return null;const detected=detectContext(state,pos,source);if(detected.type!=='headers'){pendingIncludeHeaderSuggestions=false;}
if(detected.type==='string'||detected.type==='comment'){return null;}
if(detected.type==='include-keyword'){return{from:detected.from,options:[{label:INCLUDE_DIRECTIVE,detail:'<header.h>',type:'keyword',apply:(view,completion,from,to)=>{pendingIncludeHeaderSuggestions=true;view.dispatch({changes:{from,to,insert:'#include <>'},selection:{anchor:from+'#include <'.length}});if(typeof startAutocomplete==='function'){setTimeout(()=>startAutocomplete(view),0);}}}],validFor:/^#\w*$/};}
if(detected.type==='headers'){const useStarterList=!detected.prefix&&pendingIncludeHeaderSuggestions;if(!detected.prefix&&!useStarterList){return null;}
const headerSuggestions=getHeaderSuggestions(source,detected.prefix,useStarterList);pendingIncludeHeaderSuggestions=false;if(!headerSuggestions.length){return null;}
return{from:detected.from,options:buildHeaderOptions(headerSuggestions,useStarterList,detected.closeDelimiter),validFor:/^[\w/]*$/};}
if(detected.type==='preprocessor'){return null;}
const word=currentWord;if(!word)return null;if(word.from===word.to&&!context.explicit){if(detected.type==='functions'){return null;}else if(detected.type==='constants'){const charBefore=state.sliceDoc(pos-1,pos);if(!['(',',',' '].includes(charBefore)){return null;}}}
if(detected.type==='functions'){const prefix=(detected.prefix||'').toLowerCase();const functionOptions=buildFunctionOptions().filter((option)=>(!prefix||fuzzyMatch(option.label.toLowerCase(),prefix)));const variableOptions=docCache.variables.filter((name)=>!prefix||fuzzyMatch(name.toLowerCase(),prefix)).map((name)=>({label:name,type:'variable',boost:-2}));const snippetOptions=Object.values(SNIPPETS).filter((snippet)=>!prefix||fuzzyMatch(snippet.label.toLowerCase(),prefix));const options=[...snippetOptions,...functionOptions,...variableOptions];return{from:word.from,options,validFor:/^\w*$/};}else if(detected.type==='constants'){const func=functionsMap[detected.funcName];if(!func){return null;}
if(!func.accepts||!func.accepts[detected.paramIndex]){return null;}
const cacheKey=detected.funcName+':'+detected.paramIndex;if(constantCache.has(cacheKey)){return{from:word.from,options:constantCache.get(cacheKey),validFor:/^\w*$/};}
const groups=func.accepts[detected.paramIndex];const options=[];const seen=new Set();for(const group of groups){const constants=constantsMap[group]||[];for(const constant of constants){if(seen.has(constant))continue;seen.add(constant);options.push({label:constant,type:'constant',apply:(view,completion,from,to)=>{view.dispatch({changes:{from,to,insert:constant},selection:{anchor:from+constant.length}});}});}}
constantCache.set(cacheKey,options);return{from:word.from,options,validFor:/^\w*$/};}
return null;}
function buildTooltipHTML(info,activeParamIndex,state){const dom=document.createElement('div');dom.className='cm-func-tooltip';let html=`<div class="tooltip-signature">${info.signature}</div>`;html+=`<div class="tooltip-scroll-area">`;if(state&&info.header){const includedHeaders=getIncludedHeaders(state);if(!includedHeaders.has(info.header.toLowerCase())){html+=`<div class="tooltip-warning">⚠ Requires #include &lt;${info.header}&gt;</div>`;}}
if(info.description){html+=`<div class="tooltip-description">${info.description}</div>`;}
if(info.params&&info.params.length>0){html+=`<div class="tooltip-params">`;info.params.forEach((param,index)=>{const parts=param.split('→');const activeClass=index===activeParamIndex?' tooltip-param-active':'';if(parts.length>1){const name=parts[0].trim();const desc=parts.slice(1).join('→').trim();html+=`<div class="tooltip-param${activeClass}"><span class="tooltip-param-name">${name}</span> <span class="tooltip-param-arrow">→</span> <span class="tooltip-param-desc">${desc}</span></div>`;}else{html+=`<div class="tooltip-param${activeClass}">${param}</div>`;}});html+=`</div>`;}
html+=`</div>`;dom.innerHTML=html;return dom;}
let showSelectionTooltip;let hideSelectionTooltip;window.setupAutocomplete=function(editorView){const{hoverTooltip,showTooltip}=cmModules.view;const{StateField,StateEffect}=cmModules.state;const{autocompletion,startCompletion,snippet}=cmModules.autocomplete;startAutocomplete=startCompletion;applySnippet=snippet;showSelectionTooltip=StateEffect.define();hideSelectionTooltip=StateEffect.define();autocompleteCacheField=StateField.define({create(state){return buildAutocompleteDocCache(state.doc);},update(value,tr){if(!tr.docChanged){return value;}
return updateAutocompleteDocCache(value,tr);}});const hoverTooltipSource=hoverTooltip((view,pos,side)=>{if(!dataLoaded)return null;const source=getAutocompleteDocCache(view.state).source;const detected=detectContext(view.state,pos,source);const word=view.state.wordAt(pos);if(!word)return null;const name=view.state.sliceDoc(word.from,word.to);const func=functionsMap[name];if(!func||!func.info)return null;const activeParamIndex=detected.type==='constants'&&detected.funcName===name?parseInt(detected.paramIndex,10):undefined;return{pos:word.from,end:word.to,above:false,create(){return{dom:buildTooltipHTML(func.info,activeParamIndex,view.state)};}};},{hoverTime:80});const selectionTooltipField=StateField.define({create(){return null;},update(tooltip,tr){if(tr.docChanged||tr.selection)return null;for(const e of tr.effects){if(e.is(hideSelectionTooltip))return null;}
for(const e of tr.effects){if(e.is(showSelectionTooltip)){const func=functionsMap[e.value.name];if(func&&func.info){return{pos:tr.state.selection.main.head,above:false,create(){return{dom:buildTooltipHTML(func.info,e.value.activeParamIndex,tr.state)};}};}}}
return tooltip;},provide:f=>showTooltip.computeN([f],s=>{const t=s.field(f);return t?[t]:[];})});const tooltipTheme=cmModules.view.EditorView.theme({".cm-tooltip":{backgroundColor:"transparent",border:"none",boxShadow:"none",padding:"0",},".cm-tooltip.cm-tooltip-autocomplete":{backgroundColor:"#1d1f23",border:"1px solid rgba(120, 142, 166, 0.16)",borderRadius:"16px",padding:"7px",boxShadow:"0 18px 30px rgba(0,0,0,0.30)",fontFamily:"'JetBrains Mono', monospace",fontSize:"13px",minWidth:"210px",maxWidth:"260px"},".cm-tooltip.cm-tooltip-autocomplete > ul":{margin:"0",padding:"0",minWidth:"196px",maxWidth:"246px"},".cm-tooltip-autocomplete ul::-webkit-scrollbar":{width:"8px"},".cm-tooltip-autocomplete ul::-webkit-scrollbar-track":{background:"transparent"},".cm-tooltip-autocomplete ul::-webkit-scrollbar-thumb":{backgroundColor:"#3f4854",borderRadius:"999px",border:"2px solid #1d1f23"},".cm-tooltip-autocomplete ul::-webkit-scrollbar-thumb:hover":{backgroundColor:"#556171"},".cm-tooltip.cm-tooltip-autocomplete > ul > li":{padding:"8px 11px",color:"#d8e0ea",borderRadius:"12px",cursor:"pointer",display:"flex",alignItems:"center",gap:"6px",lineHeight:"1.3",minHeight:"32px",margin:"2px 0"},".cm-tooltip.cm-tooltip-autocomplete > ul > li[aria-selected]":{backgroundColor:"rgba(var(--color-rgb-primary, 0, 255, 136), 0.11)",outline:"1px solid rgba(var(--color-rgb-primary, 0, 255, 136), 0.18)",color:"#ffffff",},".cm-completionIcon":{display:"flex",alignItems:"center",justifyContent:"center",width:"14px",height:"14px",marginRight:"0",opacity:"0.78",flexShrink:"0"},".cm-completionIcon-function::after":{content:"'ƒ'",color:"#c586c0",fontWeight:"bold",fontStyle:"italic",fontFamily:"serif",fontSize:"15px"},".cm-completionIcon-constant::after":{content:"'[c]'",color:"#4fc1ff",fontFamily:"monospace",fontSize:"11px",letterSpacing:"-1px"},".cm-completionIcon-variable::after":{content:"'v'",color:"#9cdcfe",fontFamily:"monospace",fontSize:"12px"},".cm-completionMatchedText":{color:"#86f7b2",textDecoration:"none",fontWeight:"bold",},".cm-completionLabel":{fontSize:"13px",lineHeight:"1.25"},".cm-completionDetail":{color:"#8b97a6",fontStyle:"normal",marginLeft:"4px",fontSize:"11px",fontFamily:"'JetBrains Mono', monospace"},".cm-func-tooltip":{backgroundColor:"#252526",border:"1px solid #454545",borderRadius:"6px",overflow:"hidden",maxWidth:"400px",maxHeight:"220px",display:"flex",flexDirection:"column",boxShadow:"0 8px 24px rgba(0,0,0,0.5)",fontFamily:"system-ui, -apple-system, sans-serif",color:"#cccccc",},".tooltip-signature":{backgroundColor:"#1e1e1e",borderBottom:"1px solid #454545",padding:"8px 12px",color:"#dcdcaa",fontFamily:"'JetBrains Mono', monospace",fontSize:"12.5px",flexShrink:0},".tooltip-scroll-area":{overflowY:"auto",flex:1},".tooltip-warning":{color:"#ffcc00",fontSize:"11px",padding:"8px 12px",borderBottom:"1px solid #454545"},".tooltip-scroll-area::-webkit-scrollbar":{width:"6px"},".tooltip-scroll-area::-webkit-scrollbar-track":{background:"transparent"},".tooltip-scroll-area::-webkit-scrollbar-thumb":{backgroundColor:"#424242",borderRadius:"4px"},".tooltip-scroll-area::-webkit-scrollbar-thumb:hover":{backgroundColor:"#4f4f4f"},".tooltip-description":{padding:"10px 12px 6px",color:"#cccccc",fontSize:"12px",lineHeight:"1.4",},".tooltip-params":{padding:"4px 12px 10px",display:"flex",flexDirection:"column",gap:"6px",},".tooltip-param":{color:"#cccccc",fontSize:"12px",lineHeight:"1.4",display:"flex",alignItems:"flex-start",gap:"6px"},".tooltip-param-name":{color:"#9cdcfe",fontFamily:"'JetBrains Mono', monospace",fontSize:"12px",fontWeight:"normal",whiteSpace:"nowrap",},".tooltip-param-active .tooltip-param-name":{color:"#ffcc00",fontWeight:"bold"},".tooltip-param-arrow":{color:"#858585",fontSize:"12px",},".tooltip-param-desc":{flex:"1"}});const tooltipHideListener=cmModules.view.EditorView.domEventHandlers({mousedown(e,view){view.dispatch({effects:hideSelectionTooltip.of()});return false;}});const autocompleteCompartment=new cmModules.state.Compartment();const tooltipCompartment=new cmModules.state.Compartment();const appSettings=(typeof loadAppSettings==='function')?loadAppSettings():{};const initAc=appSettings?.editor?.autocomplete!==false;const initTt=appSettings?.editor?.hoverTooltips!==false;const acExtension=autocompletion({override:[getCompletions]});const ttExtension=[hoverTooltipSource,selectionTooltipField,tooltipHideListener];editorView.dispatch({effects:cmModules.state.StateEffect.appendConfig.of([autocompleteCacheField,autocompleteCompartment.of(initAc?acExtension:[]),tooltipCompartment.of(initTt?ttExtension:[]),tooltipTheme])});document.addEventListener('editor-settings-changed',(event)=>{const settings=event.detail?.settings?.editor||{};const enableAc=settings.autocomplete!==false;const enableTt=settings.hoverTooltips!==false;editorView.dispatch({effects:[autocompleteCompartment.reconfigure(enableAc?acExtension:[]),tooltipCompartment.reconfigure(enableTt?ttExtension:[])]});});};;let cmModules=null;const editorPromise=import('/static/js/compiler/codemirror.bundle.v1.js');async function loadCodeMirror(){Logger.info('Loading CodeMirror via local bundle...');updateLoadingProgress(30);try{const bundle=await editorPromise;cmModules={cm:bundle.cmCore,view:bundle.cmView,state:bundle.cmCore,language:bundle.cmLanguage,cpp:bundle.cmCpp,commands:bundle.cmCommands,search:bundle.cmSearch,autocomplete:bundle.cmAutocomplete,highlight:bundle.lezerHighlight};Logger.success('CodeMirror bundle loaded');updateLoadingProgress(70);return true;}catch(error){Logger.error('Failed to load CodeMirror bundle',error);throw error;}}
async function loadAllScripts(){try{Logger.info('Loading dependencies with production preload flow...');updateEditorLoadingState('Loading editor shell...','Preparing compiler assets and workspace UI.');updateLoadingProgress(10);await initializeResourcesFromManifest();updateEditorLoadingState('Loading CodeMirror...','Fetching the local editor bundle and language support.');await loadCodeMirror();scriptsLoaded.codemirror=true;updateLoadingProgress(50);await initializeEditor();updateEditorLoadingState('Loading starter code...','Restoring your draft or fetching the default demo.');await loadDefaultCode();updateEditorLoadingState('Preparing DOS runner...','Mounting the terminal frame after the editor is ready.');await ensureDosRunnerFrame();updateEditorLoadingState('Warming compiler runtime...','Caching DOS runtime files in the background.');const queuePreload=()=>{startPreload().then(()=>updateCacheStatus()).catch((error)=>Logger.warn(`Compiler preload skipped: ${error.message}`));};if(window.requestIdleCallback){window.requestIdleCallback(queuePreload);}else{Promise.resolve().then(queuePreload);}
const editorLoadingOverlay=document.getElementById('editor-loading-overlay');if(editorLoadingOverlay){editorLoadingOverlay.classList.add('hidden');}
updateLoadingProgress(100);return true;}catch(error){Logger.error('Failed to load dependencies',error);const editorContainer=document.getElementById('editor');if(editorContainer){editorContainer.innerHTML=`<div style="color: #ff6b6b; text-align: center; padding: 2rem; border: 1px solid #ff6b6b; border-radius: 8px; margin: 2rem;">
                <p style="margin-bottom: 1rem; font-size: 1.1rem;">⚠ Editor failed to load. This may be a network issue. Please refresh the page.</p>
                <button onclick="window.location.reload()" style="padding: 0.5rem 1rem; cursor: pointer; background: #ff6b6b; color: #fff; border: none; border-radius: 4px; font-weight: bold;">Refresh</button>
            </div>`;}
return false;}}
function updateLoadingProgress(percent){const normalized=Math.max(0,Math.min(100,Math.round(Number(percent)||0)));if(loadingProgressBar){loadingProgressBar.style.width=`${normalized}%`;}}
demoSelect.addEventListener('change',async(e)=>{const selectedDemo=e.target.value;if(selectedDemo===lastLoadedDemo){Logger.info(`Reloading ${selectedDemo} demo (force refresh)`);await loadDemoFile(selectedDemo,true);}else{await loadDemoFile(selectedDemo,false);}});async function loadDemoFile(demoKey,forceReload=false){if(!editor)return;try{if(!forceReload){const cachedCode=DemoCache.get(demoKey);if(cachedCode){Logger.info(`Loading ${demoKey} demo from cache`);editor.setValue(cachedCode);lastLoadedDemo=demoKey;localStorage.removeItem("tc_code");updateEditorInfo();updateSaveIndicator();Logger.success(`Loaded ${demoKey} demo from cache`);return;}}
Logger.info(`Loading ${demoKey} demo from bundled demo JSON...${forceReload ? ' (force reload)' : ''}`);await loadDemoBundle();const code=DEMO_FILE_CONTENTS[demoKey];if(typeof code!=='string'){throw new Error(`Demo file not found: ${demoKey}`);}
DemoCache.set(demoKey,code);editor.setValue(code);lastLoadedDemo=demoKey;{const activeKey=CLOUD_STATE.activeFileKey||'root/main.cpp';const[folder,filename]=activeKey.split('/');setLocalDraftImmediate(folder,filename,code);}
if(isUserLoggedIn){CLOUD_STATE.lastSavedHash=null;}
scheduleAutosave();updateEditorInfo();updateSaveIndicator();Logger.success(`Loaded ${demoKey} demo`);}catch(e){Logger.error('Error loading demo file',e);alert('Error loading demo file. Please check your connection or try offline mode.');}}
let editorStyleCompartment=null;let fontSizeCompartment=null;let wordWrapCompartment=null;let lineNumbersCompartment=null;let bracketMatchCompartment=null;let activeLineCompartment=null;let cmView=null;function createEditorWrapper(view){return{getValue(){return view.state.doc.toString();},setValue(text){view.dispatch({changes:{from:0,to:view.state.doc.length,insert:text||''}});},clearSelection(){view.dispatch({selection:{anchor:0}});},moveCursorTo(line,col){const targetLine=Math.min(line+1,view.state.doc.lines);const lineInfo=view.state.doc.line(targetLine);const pos=lineInfo.from+Math.min(col,lineInfo.length);view.dispatch({selection:{anchor:pos}});},focus(){view.focus();},setFontSize(sizeStr){if(!fontSizeCompartment)return;const{EditorView}=cmModules.view;view.dispatch({effects:fontSizeCompartment.reconfigure(EditorView.theme({'.cm-content, .cm-gutters':{fontSize:sizeStr}}))});},resize(){view.requestMeasure();},requestMeasure(){view.requestMeasure();},on(event,callback){Logger.warn('editor.on("'+event+'") is a no-op in the CodeMirror 6 wrapper. Use EditorView.updateListener instead.');},renderer:{updateFull(){view.requestMeasure();}},};}
function createVsCodeEditorStyleExtension(){const{EditorView}=cmModules.view;const{HighlightStyle,syntaxHighlighting}=cmModules.language;const{tags}=cmModules.highlight;const highlight=HighlightStyle.define([{tag:tags.keyword,color:'var(--syn-keyword)'},{tag:tags.name,color:'var(--syn-name)'},{tag:tags.typeName,color:'var(--syn-type)'},{tag:tags.variableName,color:'var(--syn-name)'},{tag:tags.propertyName,color:'var(--syn-property)'},{tag:tags.function(tags.variableName),color:'var(--syn-property)'},{tag:tags.string,color:'var(--syn-string)'},{tag:tags.number,color:'var(--syn-number)'},{tag:tags.bool,color:'var(--syn-number)'},{tag:tags.comment,color:'var(--syn-comment)'},{tag:tags.operator,color:'var(--syn-keyword)'},{tag:tags.bracket,color:'var(--syn-name)'},{tag:tags.meta,color:'var(--syn-meta)'},{tag:tags.processingInstruction,color:'var(--syn-meta)'},{tag:tags.definition(tags.variableName),color:'var(--syn-property)'},{tag:tags.macroName,color:'var(--syn-property)'},]);return[EditorView.theme({'&':{backgroundColor:'transparent'},'.cm-scroller':{backgroundColor:'transparent'},'.cm-content':{color:'var(--text-primary)'},'.cm-cursor':{borderLeftColor:'var(--primary)'},'.cm-activeLine':{backgroundColor:'var(--vscode-line-bg)'},'.cm-activeLineGutter':{backgroundColor:'var(--vscode-line-bg)'},'.cm-gutters':{backgroundColor:'var(--vscode-sidebar)',color:'var(--text-muted)',borderRight:'1px solid var(--vscode-border)'},'.cm-selectionBackground':{backgroundColor:'rgba(var(--color-rgb-primary, 0, 255, 136), 0.15) !important'},'&.cm-focused .cm-selectionBackground':{backgroundColor:'rgba(var(--color-rgb-primary, 0, 255, 136), 0.15) !important'},'.cm-matchingBracket':{backgroundColor:'rgba(var(--color-rgb-primary, 0, 255, 136), 0.25)',outline:'1px solid rgba(var(--color-rgb-primary, 0, 255, 136), 0.4)'},}),syntaxHighlighting(highlight)];}
function createSelectedMatchHighlightExtension(){const{Decoration,ViewPlugin}=cmModules.view;const selectedMark=Decoration.mark({class:'cm-selectionMatch cm-selectionMatch-main'});return ViewPlugin.fromClass(class{constructor(view){this.decorations=this.buildDecorations(view);}
update(update){if(update.selectionSet||update.docChanged){this.decorations=this.buildDecorations(update.view);}}
buildDecorations(view){const main=view.state.selection.main;if(main.empty){return Decoration.none;}
return Decoration.set([selectedMark.range(main.from,main.to)],true);}},{decorations:(plugin)=>plugin.decorations});}
function createEmptyEditorTipExtension(){const{EditorView,placeholder}=cmModules.view;const tip=document.createElement('div');tip.className='cm-empty-tip';tip.setAttribute('aria-hidden','true');tip.innerHTML='Tip: Type <span class="cm-empty-tip-highlight">boilerplate</span> to insert the graphics.h template';return[placeholder(tip),EditorView.theme({'.cm-empty-tip':{color:'var(--text-muted)',opacity:'0.78',fontFamily:"'JetBrains Mono', 'Fira Code', monospace",fontSize:'14px',lineHeight:'1.6',pointerEvents:'none',userSelect:'none',whiteSpace:'normal',padding:'0',maxWidth:'520px'},'.cm-empty-tip-highlight':{color:'var(--primary)',fontWeight:'700'}})];}
function createBracketClosingExtension(){const{EditorView,keymap}=cmModules.view;const pairs=new Map([['(',')'],['[',']'],['{','}'],['"','"'],["'","'"],]);const closingChars=new Set([...pairs.values()]);const inputHandler=EditorView.inputHandler.of((view,from,to,text)=>{if(text.length!==1)return false;const close=pairs.get(text);if(close){const selectedText=view.state.sliceDoc(from,to);view.dispatch({changes:{from,to,insert:text+selectedText+close},selection:{anchor:from+1,head:from+1+selectedText.length},userEvent:'input.type'});return true;}
if(closingChars.has(text)&&from===to&&view.state.sliceDoc(from,from+1)===text){view.dispatch({selection:{anchor:from+1},userEvent:'select'});return true;}
return false;});const deletePairedBrackets=({state,dispatch})=>{const range=state.selection.main;if(!range.empty||range.from===0||range.from>=state.doc.length){return false;}
const before=state.sliceDoc(range.from-1,range.from);const after=state.sliceDoc(range.from,range.from+1);if(pairs.get(before)!==after){return false;}
dispatch(state.update({changes:{from:range.from-1,to:range.from+1,insert:''},selection:{anchor:range.from-1},userEvent:'delete.backward'}));return true;};const splitBrackets=({state,dispatch})=>{const range=state.selection.main;if(!range.empty||range.from===0||range.from>=state.doc.length){return false;}
const before=state.sliceDoc(range.from-1,range.from);const after=state.sliceDoc(range.from,range.from+1);if(pairs.get(before)===after&&(before==='{'||before==='['||before==='(')){const line=state.doc.lineAt(range.from);const indentStr=line.text.match(/^\s*/)[0];const indentUnit="    ";const insert='\n'+indentStr+indentUnit+'\n'+indentStr;dispatch(state.update({changes:{from:range.from,to:range.from,insert:insert},selection:{anchor:range.from+1+indentStr.length+indentUnit.length},userEvent:'input.type'}));return true;}
return false;};return[inputHandler,keymap.of([{key:'Backspace',run:deletePairedBrackets},{key:'Enter',run:splitBrackets}])];}
async function initializeEditor(){if(!scriptsLoaded.codemirror||!cmModules){setTimeout(initializeEditor,100);return;}
const{EditorView,keymap,lineNumbers,highlightActiveLine,highlightActiveLineGutter,drawSelection}=cmModules.view;const{EditorState,Compartment}=cmModules.state;const{indentOnInput,bracketMatching}=cmModules.language;const{cpp}=cmModules.cpp;const{defaultKeymap,indentWithTab,history,historyKeymap}=cmModules.commands;const{highlightSelectionMatches}=cmModules.search;const defaultEditorSettings=(typeof APP_SETTINGS_DEFAULTS!=='undefined'&&APP_SETTINGS_DEFAULTS.editor)?APP_SETTINGS_DEFAULTS.editor:{fontSize:16,wordWrap:true,lineNumbers:true,bracketMatching:true,activeLine:true};const appSettings=(typeof loadAppSettings==='function')?loadAppSettings():{editor:{...defaultEditorSettings}};const initialEditorSettings={...defaultEditorSettings,...(appSettings.editor||{})};const initialFontSize=Math.max(10,Math.min(32,Number.parseInt(initialEditorSettings.fontSize,10)||defaultEditorSettings.fontSize));editorStyleCompartment=new Compartment();fontSizeCompartment=new Compartment();wordWrapCompartment=new Compartment();lineNumbersCompartment=new Compartment();bracketMatchCompartment=new Compartment();activeLineCompartment=new Compartment();const heavyFeaturesCompartment=new Compartment();const extensions=[lineNumbersCompartment.of(initialEditorSettings.lineNumbers?lineNumbers():[]),activeLineCompartment.of(initialEditorSettings.activeLine?[highlightActiveLine(),highlightActiveLineGutter()]:[]),drawSelection(),indentOnInput(),cpp(),createBracketClosingExtension(),keymap.of([...defaultKeymap,indentWithTab,]),editorStyleCompartment.of(createVsCodeEditorStyleExtension()),fontSizeCompartment.of(EditorView.theme({'.cm-content, .cm-gutters':{fontSize:`${initialFontSize}px`}})),EditorView.theme({'.cm-content':{fontFamily:"'JetBrains Mono', 'Fira Code', monospace"},'.cm-gutters':{fontFamily:"'JetBrains Mono', 'Fira Code', monospace"},'&':{height:'100%'},'.cm-scroller':{overflow:'auto'}}),createEmptyEditorTipExtension(),EditorState.tabSize.of(4),cmModules.language.indentUnit.of("    "),wordWrapCompartment.of(initialEditorSettings.wordWrap?EditorView.lineWrapping:[]),heavyFeaturesCompartment.of([]),];const editorContainer=document.getElementById('editor');editorContainer.innerHTML='';cmView=new EditorView({state:EditorState.create({doc:'',extensions:extensions}),parent:editorContainer});if(typeof setupAutocomplete==='function'){setupAutocomplete(cmView);}
editor=createEditorWrapper(cmView);const increaseFontBtn=document.getElementById('increase-font-btn');const decreaseFontBtn=document.getElementById('decrease-font-btn');const fontSizeDisplay=document.getElementById('font-size-display');function updateFontSizeDisplay(size){if(fontSizeDisplay){fontSizeDisplay.textContent=size;}}
function requestFontSizeChange(delta){if(typeof loadAppSettings!=='function')return;const currentSettings=loadAppSettings();const currentFontSize=Math.max(10,Math.min(32,Number.parseInt(currentSettings.editor.fontSize,10)||defaultEditorSettings.fontSize));const nextFontSize=Math.max(10,Math.min(32,currentFontSize+delta));document.dispatchEvent(new CustomEvent('editor-font-size-change-requested',{detail:{fontSize:nextFontSize}}));}
updateFontSizeDisplay(initialFontSize);if(increaseFontBtn&&decreaseFontBtn){increaseFontBtn.addEventListener('click',()=>{requestFontSizeChange(1);});decreaseFontBtn.addEventListener('click',()=>{requestFontSizeChange(-1);});}
document.addEventListener('editor-settings-changed',(event)=>{const latestFontSize=event.detail?.settings?.editor?.fontSize;if(Number.isFinite(latestFontSize)){updateFontSizeDisplay(latestFontSize);}});const copyCodeBtn=document.getElementById('copy-code-btn');if(copyCodeBtn){copyCodeBtn.addEventListener('click',async()=>{const code=editor.getValue();if(!code)return;try{await navigator.clipboard.writeText(code);const originalTitle=copyCodeBtn.title;const originalHTML=copyCodeBtn.innerHTML;copyCodeBtn.classList.add('success');copyCodeBtn.innerHTML=`<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>`;copyCodeBtn.title='Copied!';setTimeout(()=>{copyCodeBtn.classList.remove('success');copyCodeBtn.innerHTML=originalHTML;copyCodeBtn.title=originalTitle;},2000);Logger.info('Code copied to clipboard');}catch(err){Logger.error('Failed to copy code',err);}});}
const executeHeavyFeatures=()=>{const{history,historyKeymap}=cmModules.commands;const{highlightSelectionMatches}=cmModules.search;const{bracketMatching}=cmModules.language;const heavyExtensions=[bracketMatchCompartment.of(initialEditorSettings.bracketMatching?bracketMatching():[]),history(),highlightSelectionMatches(),createSelectedMatchHighlightExtension(),keymap.of([...historyKeymap])];cmView.dispatch({effects:heavyFeaturesCompartment.reconfigure(heavyExtensions)});Logger.info('Heavy editor extensions loaded via idle callback');};if(window.requestIdleCallback){window.requestIdleCallback(executeHeavyFeatures);}else{setTimeout(executeHeavyFeatures,300);}
let uiUpdateTimer=null;const UI_UPDATE_DEBOUNCE_MS=150;const changeListener=EditorView.updateListener.of((update)=>{if(!update.docChanged)return;metrics.editor.changeCount++;metrics.editor.lastChangeAt=Date.now();DIRTY_FLAG.isDirty=true;{const activeKey=CLOUD_STATE.activeFileKey||'root/main.cpp';const[_f,_n]=activeKey.split('/');setLocalDraftImmediate(_f,_n,update.state.doc.toString());}
scheduleAutosave();if(uiUpdateTimer){clearTimeout(uiUpdateTimer);}
uiUpdateTimer=setTimeout(()=>{updateEditorInfo();updateSaveIndicator();uiUpdateTimer=null;},UI_UPDATE_DEBOUNCE_MS);});cmView.dispatch({effects:cmModules.state.StateEffect.appendConfig.of(changeListener)});setTimeout(()=>{editor.focus();editorWrapper.classList.add('active');},100);document.dispatchEvent(new CustomEvent('editor-ready',{detail:{cmView}}));Logger.success('Editor ready (CodeMirror 6)');}
function updateEditorInfo(){if(!editor||!editorInfo)return;const code=editor.getValue();const lines=code.split('\n').length;const chars=code.length;editorInfo.textContent=`Lines: ${lines} | Chars: ${chars}`;}
async function loadDefaultCode(){if(isUserLoggedIn){await refreshCloudFiles();const activeKey=CLOUD_STATE.activeFileKey||'root/main.cpp';const[folder,filename]=activeKey.split('/');await openFile(folder,filename,{skipSave:true});DIRTY_FLAG.isDirty=false;updateSaveIndicator();return;}
const draft=await getLocalDraft('root','main.cpp');if(draft!==null){editor.setValue(draft);SAVE_STATE.lastSavedHash=await computeSha256(draft);updateEditorInfo();DIRTY_FLAG.isDirty=false;updateSaveIndicator();Logger.info('Restored guest draft from IndexedDB');return;}
const savedCode=localStorage.getItem("tc_code");if(savedCode){editor.setValue(savedCode);await setLocalDraft('root','main.cpp',savedCode).catch(()=>{});localStorage.removeItem("tc_code");SAVE_STATE.lastSavedHash=await computeSha256(savedCode);updateEditorInfo();DIRTY_FLAG.isDirty=false;updateSaveIndicator();Logger.info('Migrated saved code from localStorage to IndexedDB');return;}
await loadDemoFile('graphics-demo',false);DIRTY_FLAG.isDirty=false;updateSaveIndicator();}
clearBtn.addEventListener('click',()=>{if(confirm('Are you sure you want to clear the editor?')){editor.setValue('');{const activeKey=CLOUD_STATE.activeFileKey||'root/main.cpp';const[folder,filename]=activeKey.split('/');setLocalDraftImmediate(folder,filename,'');}
if(!isUserLoggedIn){localStorage.removeItem("tc_code");}
CLOUD_STATE.lastSavedHash=null;lastLoadedDemo='';updateEditorInfo();updateSaveIndicator();Logger.info('Editor cleared');}});;var isEditorFullscreen=false;var isTerminalFullscreen=false;(function(){'use strict';const mobileTabEditor=document.getElementById('mobile-tab-editor');const mobileTabOutput=document.getElementById('mobile-tab-output');const localEditorWrapper=document.getElementById('editor-wrapper');const localTerminalWrapper=document.getElementById('terminal-wrapper');const localKeyboardBlocker=document.getElementById('keyboard-blocker');const sidebarContainer=document.querySelector('.sidebar-container');const sidebar=document.getElementById('sidebar');const sidebarOverlay=document.getElementById('sidebar-overlay');const sidebarToggle=document.getElementById('files-header-btn');const refreshBtn=document.getElementById('refresh-btn');const sidebarCollapseBtn=document.getElementById('sidebar-collapse-btn');const explorerActivityBtn=document.getElementById('explorer-activity-btn');const cloudPromoView=document.getElementById('cloud-promo-view');const fileExplorerView=document.getElementById('file-explorer-view');const authSection=document.getElementById('auth-section');const userProfileSection=document.getElementById('user-profile-section');const userProfileBtn=document.getElementById('user-profile-btn');const userProfileDropdown=document.getElementById('user-profile-dropdown');const userAvatar=document.getElementById('user-avatar');const userName=document.getElementById('user-name');const userEmail=document.getElementById('user-email');const signoutBtn=document.getElementById('signout-btn');const newFolderBtn=document.getElementById('new-folder-btn');const newFileBtn=document.getElementById('new-file-btn');function isMobileView(){return window.innerWidth<=768;}
function setVisible(element,visible){if(!element)return;element.classList.toggle('hidden',!visible);}
function syncMobileSidebarState(){const isOpen=isMobileView()&&sidebar&&sidebar.classList.contains('open');document.body.classList.toggle('sidebar-open-mobile',isOpen);if(sidebarOverlay){sidebarOverlay.classList.toggle('active',isOpen);}}
function getCurrentMobileTab(){return document.body.classList.contains('mobile-tab-output')?'output':'editor';}
function setPanelDisplay(element,value){if(!element)return;if(value){element.style.setProperty('display',value,'important');}else{element.style.removeProperty('display');}}
function applyMobileTabLayout(tab){if(!localEditorWrapper||!localTerminalWrapper)return;if(!isMobileView()){document.body.classList.remove('mobile-tab-output');setPanelDisplay(localEditorWrapper,'');setPanelDisplay(localTerminalWrapper,'');if(sidebarContainer){setPanelDisplay(sidebarContainer,'');}
return;}
const sidebarOpen=Boolean(sidebar&&sidebar.classList.contains('open'));if(sidebarOpen){setPanelDisplay(localEditorWrapper,'none');setPanelDisplay(localTerminalWrapper,'none');if(sidebarContainer){setPanelDisplay(sidebarContainer,'flex');}
return;}
if(sidebarContainer){setPanelDisplay(sidebarContainer,'none');}
if(tab==='output'){document.body.classList.add('mobile-tab-output');setPanelDisplay(localEditorWrapper,'none');setPanelDisplay(localTerminalWrapper,'flex');return;}
document.body.classList.remove('mobile-tab-output');setPanelDisplay(localEditorWrapper,'flex');setPanelDisplay(localTerminalWrapper,'none');}
function releaseTerminalFocus(){const iframe=document.getElementById('dos-iframe');if(iframe){iframe.blur();if(iframe.contentWindow){iframe.contentWindow.postMessage({type:'BLUR'},'*');}}
if(document.activeElement&&document.activeElement!==document.body){document.activeElement.blur();}
terminalFocused=false;localTerminalWrapper?.classList.remove('terminal-active');localEditorWrapper?.classList.add('active');if(iframe){localKeyboardBlocker?.classList.add('active');}}
function switchMobileTab(tab){if(!mobileTabEditor||!mobileTabOutput)return;if(isMobileView()&&sidebar&&sidebar.classList.contains('open')){sidebar.classList.remove('open');syncMobileSidebarState();}
mobileTabEditor.classList.remove('active');mobileTabOutput.classList.remove('active');mobileTabEditor.setAttribute('aria-selected','false');mobileTabOutput.setAttribute('aria-selected','false');if(tab==='output'){mobileTabOutput.classList.add('active');mobileTabOutput.setAttribute('aria-selected','true');}else{mobileTabEditor.classList.add('active');mobileTabEditor.setAttribute('aria-selected','true');releaseTerminalFocus();}
applyMobileTabLayout(tab);requestAnimationFrame(()=>{if(editor&&editor.requestMeasure)editor.requestMeasure();window.dispatchEvent(new Event('resize'));if(tab==='editor'&&editor){setTimeout(()=>editor.focus(),80);}});}
function toggleEditorFullscreen(forceState){isEditorFullscreen=typeof forceState==='boolean'?forceState:!isEditorFullscreen;const svgIcon=document.querySelector('#fullscreen-editor-btn svg');localEditorWrapper?.classList.toggle('fullscreen',isEditorFullscreen);localTerminalWrapper?.classList.toggle('hidden',isEditorFullscreen);if(svgIcon){svgIcon.innerHTML=isEditorFullscreen?'<path d="M5 16h3v3h2v-5H5v2zm3-8H5v2h5V5H8v3zm6 11h2v-3h3v-2h-5v5zm2-11V5h-2v5h5V8h-3z"/>':'<path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/>';}
setTimeout(()=>{if(editor&&editor.requestMeasure){editor.requestMeasure();}},100);}
function toggleTerminalFullscreen(){isTerminalFullscreen=!isTerminalFullscreen;const fsBtn=document.getElementById('fullscreen-terminal-btn');const svgIcon=fsBtn?fsBtn.querySelector('svg'):null;const terminalZoomControls=document.getElementById('terminal-zoom-controls');localTerminalWrapper?.classList.toggle('fullscreen',isTerminalFullscreen);localEditorWrapper?.classList.toggle('hidden',isTerminalFullscreen);if(svgIcon){svgIcon.innerHTML=isTerminalFullscreen?'<path d="M5 16h3v3h2v-5H5v2zm3-8H5v2h5V5H8v3zm6 11h2v-3h3v-2h-5v5zm2-11V5h-2v5h5V8h-3z"/>':'<path d="M7 14H5v5h5v-2H7v-3zm-2-4h2V7h3V5H5v5zm12 7h-3v2h5v-5h-2v3zM14 5v2h3v3h2V5h-5z"/>';}
if(isTerminalFullscreen){if(typeof updateTerminalZoom==='function'){currentTerminalZoom=1.2;updateTerminalZoom(0);}
if(terminalZoomControls)terminalZoomControls.classList.remove('hidden');}else{if(terminalZoomControls)terminalZoomControls.classList.add('hidden');if(typeof resetTerminalZoom==='function'){resetTerminalZoom();}}
if(fsBtn)fsBtn.blur();setTimeout(()=>{if(document.getElementById('dos-iframe')){window.dispatchEvent(new Event('resize'));}
if(isTerminalFullscreen&&typeof focusTerminal==='function'){focusTerminal();}},100);}
function focusTerminal(){const iframe=document.getElementById('dos-iframe');if(!iframe)return;terminalFocused=true;iframe.focus();if(iframe.contentWindow){iframe.contentWindow.postMessage({type:'FOCUS'},'*');}
localTerminalWrapper?.classList.add('terminal-active');localEditorWrapper?.classList.remove('active');localKeyboardBlocker?.classList.remove('active');}
function focusEditor(){terminalFocused=false;if(document.activeElement&&document.activeElement.tagName==='IFRAME'){document.activeElement.blur();}
if(editor){editor.focus();}
localTerminalWrapper?.classList.remove('terminal-active');localEditorWrapper?.classList.add('active');if(document.getElementById('dos-iframe')){localKeyboardBlocker?.classList.add('active');}}
function toggleDesktopSidebar(){if(!sidebar)return;sidebar.classList.toggle('collapsed');sidebar.style.width='';sidebar.style.minWidth='';if(sidebarToggle){sidebarToggle.classList.toggle('sidebar-active',!sidebar.classList.contains('collapsed'));}
setTimeout(()=>{if(editor&&editor.requestMeasure){editor.requestMeasure();}
window.dispatchEvent(new Event('resize'));},350);}
function updateLoginUI(loggedIn,user){isUserLoggedIn=loggedIn;currentUser=user;const sidebarView=typeof window.getSidebarView==='function'?window.getSidebarView():'explorer';const explorerActions=document.querySelector('.explorer-actions');const showExplorerFiles=sidebarView==='explorer'&&loggedIn;const showExplorerPromo=sidebarView==='explorer'&&!loggedIn;if(loggedIn&&user){setVisible(cloudPromoView,false);setVisible(fileExplorerView,showExplorerFiles);if(refreshBtn){setVisible(refreshBtn,true);refreshBtn.disabled=false;}
setVisible(newFolderBtn,true);setVisible(newFileBtn,true);setVisible(explorerActions,showExplorerFiles);setVisible(authSection,false);setVisible(userProfileSection,true);const displayName=user.user_metadata?.full_name||user.display_name||user.name||user.email||'Account';const email=user.email||user.user_metadata?.email||'';const avatarUrl=user.avatar_url||user.user_metadata?.avatar_url||'';if(userName)userName.textContent=displayName;if(userEmail)userEmail.textContent=email;if(userAvatar){userAvatar.innerHTML=avatarUrl?`<img src="${avatarUrl}" alt="${displayName}" referrerpolicy="no-referrer" style="width:100%;height:100%;border-radius:50%;object-fit:cover;display:block;">`:displayName.charAt(0).toUpperCase();}
Logger.success(`Signed in as ${displayName}`);}else{setVisible(cloudPromoView,showExplorerPromo);setVisible(fileExplorerView,false);if(refreshBtn){setVisible(refreshBtn,false);refreshBtn.disabled=true;}
setVisible(newFolderBtn,false);setVisible(newFileBtn,false);setVisible(document.querySelector('.explorer-actions'),false);setVisible(authSection,true);setVisible(userProfileSection,false);CLOUD_STATE.files.clear();CLOUD_STATE.folders=new Set(['root']);CLOUD_STATE.folderNameToId=new Map();CLOUD_STATE.folderIdToName=new Map();CLOUD_STATE.openTabs=[];CLOUD_STATE.activeFileKey='root/main.cpp';CLOUD_STATE.selectedFolderKey='root';updateSaveIndicator();}
document.dispatchEvent(new CustomEvent('auth-state-changed',{detail:{loggedIn,user}}));}
function handleResize(){if(!isMobileView()){document.body.classList.remove('mobile-tab-output','sidebar-open-mobile');if(isEditorFullscreen){toggleEditorFullscreen(false);}
applyMobileTabLayout('editor');}else{syncMobileSidebarState();applyMobileTabLayout(document.body.classList.contains('mobile-tab-output')?'output':'editor');}
if(editor&&editor.requestMeasure){editor.requestMeasure();}}
if(mobileTabEditor){mobileTabEditor.addEventListener('click',()=>switchMobileTab('editor'));}
if(mobileTabOutput){mobileTabOutput.addEventListener('click',()=>switchMobileTab('output'));}
document.getElementById('fullscreen-editor-btn')?.addEventListener('click',()=>{toggleEditorFullscreen();});document.getElementById('fullscreen-terminal-btn')?.addEventListener('click',toggleTerminalFullscreen);localKeyboardBlocker?.addEventListener('click',focusTerminal);localTerminalWrapper?.addEventListener('click',(event)=>{const iframe=document.getElementById('dos-iframe');if(event.target===localTerminalWrapper||event.target===iframe){if(!terminalFocused){focusTerminal();}}});localEditorWrapper?.addEventListener('click',focusEditor);document.addEventListener('click',(event)=>{if(!terminalFocused)return;if(!terminalWrapper.contains(event.target)){focusEditor();}},true);document.addEventListener('keydown',(event)=>{if(event.key!=='Escape')return;const activeEl=document.activeElement;if(activeEl&&(activeEl.tagName==='TEXTAREA'||activeEl.tagName==='INPUT')){if(sidebar&&sidebar.contains(activeEl)){return;}}
event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();focusEditor();},true);if(sidebarToggle){sidebarToggle.addEventListener('click',()=>{if(isMobileView()){sidebar.classList.toggle('open');syncMobileSidebarState();applyMobileTabLayout(getCurrentMobileTab());return;}
toggleDesktopSidebar();});}
if(sidebarOverlay){sidebarOverlay.addEventListener('click',()=>{sidebar.classList.remove('open');syncMobileSidebarState();applyMobileTabLayout(getCurrentMobileTab());});}
sidebarCollapseBtn?.addEventListener('click',toggleDesktopSidebar);if(explorerActivityBtn){explorerActivityBtn.addEventListener('click',()=>{const currentSidebarView=typeof window.getSidebarView==='function'?window.getSidebarView():'explorer';if(currentSidebarView!=='explorer'){document.dispatchEvent(new CustomEvent('request-show-explorer'));if(sidebar.classList.contains('collapsed')){sidebar.classList.remove('collapsed');setTimeout(()=>{if(editor&&editor.requestMeasure)editor.requestMeasure();},350);}
return;}
toggleDesktopSidebar();});}
refreshBtn?.addEventListener('click',()=>refreshCloudFiles(true));if(newFileBtn){newFileBtn.addEventListener('click',()=>{if(!isUserLoggedIn)return;const filename=prompt('Enter a file name (e.g., mycode.cpp):\nOnly letters, numbers, dots, underscores, and dashes are allowed.');if(filename&&filename.trim()){createNewFile(filename.trim());}});}
if(userProfileBtn){userProfileBtn.addEventListener('click',(event)=>{event.stopPropagation();userProfileDropdown.classList.toggle('open');});}
document.addEventListener('click',(event)=>{if(!userProfileDropdown||!userProfileBtn)return;if(!userProfileDropdown.contains(event.target)&&!userProfileBtn.contains(event.target)){userProfileDropdown.classList.remove('open');}});signoutBtn?.addEventListener('click',async()=>{userProfileDropdown.classList.remove('open');await signOut();});window.addEventListener('resize',handleResize);window.addEventListener('orientationchange',()=>{setTimeout(handleResize,300);});window.isMobileView=isMobileView;window.syncMobileSidebarState=syncMobileSidebarState;window.switchMobileTab=switchMobileTab;window.toggleEditorFullscreen=toggleEditorFullscreen;window.focusTerminal=focusTerminal;window.focusEditor=focusEditor;window.updateLoginUI=updateLoginUI;applyMobileTabLayout('editor');updateLoginUI(false);(function initFloatingRunBtn(){const floatBtn=document.getElementById('floating-run-btn');const floatTooltip=document.getElementById('floating-run-btn-tooltip');if(!floatBtn)return;if(isMobileView())return;const STORAGE_KEY='floating-run-btn-pos';const TOOLTIP_SHOWN_KEY='floating-run-btn-tooltip-shown';const DRAG_THRESHOLD=6;const RUN_ICON='<path d="M8 5v14l11-7z" />';const STOP_ICON='<path d="M6 6h12v12H6z" />';function getSvg(){return floatBtn.querySelector('svg');}
function positionBtn(x,y){const w=window.innerWidth;const h=window.innerHeight;const bw=floatBtn.offsetWidth||54;const bh=floatBtn.offsetHeight||54;x=Math.max(8,Math.min(w-bw-8,x));y=Math.max(8,Math.min(h-bh-8,y));floatBtn.style.left=x+'px';floatBtn.style.top=y+'px';floatBtn.style.right='auto';floatBtn.style.bottom='auto';return{x,y};}
function positionTooltip(){if(!floatTooltip)return;const bRect=floatBtn.getBoundingClientRect();const tW=floatTooltip.offsetWidth||224;const tH=floatTooltip.offsetHeight||72;const GAP=12;let tTop=bRect.top-tH-GAP;if(tTop<8)tTop=bRect.bottom+GAP;let tLeft=bRect.left+bRect.width/2-tW/2;tLeft=Math.max(8,Math.min(window.innerWidth-tW-8,tLeft));floatTooltip.style.left=tLeft+'px';floatTooltip.style.top=Math.max(8,tTop)+'px';}
function savePos(x,y){try{localStorage.setItem(STORAGE_KEY,JSON.stringify({x,y}));}catch(e){}}
function loadPos(){try{const saved=JSON.parse(localStorage.getItem(STORAGE_KEY)||'null');if(saved&&typeof saved.x==='number')return saved;}catch(e){}
return null;}
const saved=loadPos();if(saved){positionBtn(saved.x,saved.y);}else{const editorEl=document.getElementById('editor-wrapper');if(editorEl){const r=editorEl.getBoundingClientRect();positionBtn(r.right-70,r.bottom-80);}else{positionBtn(Math.round(window.innerWidth/2-25),Math.round(window.innerHeight/2-25));}}
let isDragging=false;let dragOffsetX=0;let dragOffsetY=0;let dragStartX=0;let dragStartY=0;let dragMoved=false;floatBtn.addEventListener('mousedown',(e)=>{if(e.button!==0)return;isDragging=true;dragMoved=false;const rect=floatBtn.getBoundingClientRect();dragOffsetX=e.clientX-rect.left;dragOffsetY=e.clientY-rect.top;dragStartX=e.clientX;dragStartY=e.clientY;floatBtn.style.transition='none';e.preventDefault();});document.addEventListener('mousemove',(e)=>{if(!isDragging)return;const dx=e.clientX-dragStartX;const dy=e.clientY-dragStartY;if(!dragMoved&&Math.hypot(dx,dy)<DRAG_THRESHOLD)return;dragMoved=true;positionBtn(e.clientX-dragOffsetX,e.clientY-dragOffsetY);if(floatTooltip&&floatTooltip.classList.contains('visible')){positionTooltip();}});document.addEventListener('mouseup',(e)=>{if(!isDragging)return;isDragging=false;floatBtn.style.transition='';const rect=floatBtn.getBoundingClientRect();savePos(rect.left,rect.top);if(!dragMoved){handleFloatBtnClick();}});floatBtn.addEventListener('keydown',(e)=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();handleFloatBtnClick();}});function handleFloatBtnClick(){if(typeof runProgram==='function'){runProgram();}}
function setRunning(isRunning){const svg=getSvg();if(!svg)return;floatBtn.classList.toggle('is-running',isRunning);svg.innerHTML=isRunning?STOP_ICON:RUN_ICON;floatBtn.title=isRunning?'Stop':'Compile and Run';floatBtn.setAttribute('aria-label',isRunning?'Stop':'Compile and Run');}
document.addEventListener('compiler-run-start',()=>setRunning(true));document.addEventListener('compiler-run-end',()=>setRunning(false));if(!localStorage.getItem(TOOLTIP_SHOWN_KEY)&&floatTooltip){setTimeout(()=>{positionTooltip();floatTooltip.classList.add('visible');setTimeout(()=>{floatTooltip.classList.remove('visible');try{localStorage.setItem(TOOLTIP_SHOWN_KEY,'1');}catch(e){}},5000);},1000);}
window.addEventListener('resize',()=>{const rect=floatBtn.getBoundingClientRect();positionBtn(rect.left,rect.top);if(floatTooltip&&floatTooltip.classList.contains('visible')){positionTooltip();}});})();})();;(function(){const originalLog=console.log;const originalInfo=console.info;const shouldBlock=(msg)=>{if(typeof msg!=='string')return false;if(msg.includes('extracting:'))return true;if(msg.includes('js-dos version'))return true;if(msg.includes('Copyright')&&msg.includes('DOSBox'))return true;if(msg.startsWith('CONFIG:'))return true;if(msg.startsWith('MIDI:'))return true;if(msg.startsWith('SHELL:'))return true;if(msg.includes('%')&&msg.match(/[a-f0-9-]{36}/))return true;if(msg.startsWith('[INFO]')&&msg.match(/[a-f0-9-]{36}/))return true;if(msg.includes('Resolving DosBox'))return true;if(msg.includes('DosBox resolved'))return true;if(msg.includes('Runtime is still alive'))return true;if(msg.trim()==='---')return true;return false;};console.log=function(...args){if(args.length>0&&shouldBlock(args[0])){return;}
originalLog.apply(console,args);};console.info=function(...args){if(args.length>0&&shouldBlock(args[0])){return;}
originalInfo.apply(console,args);};})();const downloadTerminalBtn=document.getElementById('download-terminal-btn');if(downloadTerminalBtn){downloadTerminalBtn.addEventListener('click',()=>{const iframe=document.getElementById('dos-iframe');if(iframe&&iframe.contentWindow){iframe.contentWindow.postMessage({type:'TAKE_SCREENSHOT',purpose:'download'},'*');}});}
var currentTerminalZoom=1.0;function updateTerminalZoom(change){const iframe=document.getElementById('dos-iframe');if(!iframe)return;let newZoom=currentTerminalZoom+change;if(newZoom<0.5)newZoom=0.5;if(newZoom>3.0)newZoom=3.0;currentTerminalZoom=newZoom;iframe.style.transform=`scale(${currentTerminalZoom})`;iframe.style.transformOrigin='center center';iframe.style.transition='transform 0.2s ease';}
function resetTerminalZoom(){currentTerminalZoom=1.0;const iframe=document.getElementById('dos-iframe');if(iframe){iframe.style.transform='scale(1)';iframe.style.transformOrigin='center center';}}
window.updateTerminalZoom=updateTerminalZoom;window.resetTerminalZoom=resetTerminalZoom;document.getElementById('increase-terminal-btn')?.addEventListener('click',()=>{updateTerminalZoom(0.1);});document.getElementById('decrease-terminal-btn')?.addEventListener('click',()=>{updateTerminalZoom(-0.1);});let currentTcZipObjectUrl=null;window.addEventListener('message',(event)=>{const iframe=document.getElementById('dos-iframe');if(!iframe||event.source!==iframe.contentWindow)return;const data=event.data||{};if(data.type==='STATUS'){if(data.status==='STARTING'){loadingText.textContent='Initializing DOS environment...';updateLoadingProgress(40);}else if(data.status==='EXTRACTING'){loadingText.textContent='Extracting compiler files...';updateLoadingProgress(60);metrics.runtime.zipExtractionStarted++;}else if(data.status==='WRITING_CODE'){loadingText.textContent='Writing source code...';updateLoadingProgress(80);}else if(data.status==='RUNNING'){loadingText.textContent='Starting program...';updateLoadingProgress(100);loading.classList.remove('active');runBtn.disabled=false;runBtn.classList.remove('loading');metrics.runtime.zipExtractionCompleted++;if(downloadTerminalBtn){downloadTerminalBtn.classList.remove('hidden');}
if(currentTcZipObjectUrl){URL.revokeObjectURL(currentTcZipObjectUrl);currentTcZipObjectUrl=null;}
setTimeout(focusTerminal,500);Logger.success('Program started successfully');document.dispatchEvent(new CustomEvent('compiler-run-success'));document.dispatchEvent(new CustomEvent('compiler-run-end'));}}else if(data.type==='PROGRESS'){updateLoadingProgress(data.percent);}else if(data.type==='COMPILATION_ERROR'){Logger.info('[Error Panel] Received COMPILATION_ERROR from iframe');if(downloadTerminalBtn){downloadTerminalBtn.classList.add('hidden');}
Logger.info('[Error Panel] Content: '+(data.content||'').substring(0,200));outputContent.textContent=data.content||'';outputContent.classList.remove('output-success');outputContent.classList.add('output-error');outputPanel.classList.remove('expanded');expandOutputBtn.classList.remove('expanded');expandOutputBtn.title='Expand panel';isOutputExpanded=false;if(!outputPanel.classList.contains('visible')){outputPanel.classList.add('visible');terminalWrapper.classList.add('has-panel');setTimeout(()=>window.dispatchEvent(new Event('resize')),310);Logger.success('[Error Panel] Panel made visible');}
if(isMobileView()&&isEditorFullscreen){toggleEditorFullscreen(false);}else if(!isMobileView()){focusEditor();}
document.dispatchEvent(new CustomEvent('compiler-compilation-error',{detail:{content:data.content||''}}));document.dispatchEvent(new CustomEvent('compiler-run-end'));}else if(data.type==='COMPILE_SUCCESS'){document.dispatchEvent(new CustomEvent('compiler-compile-success'));}else if(data.type==='ERROR'){const message=data.message||'Unknown DOS error';Logger.error('DOS Error',message);alert('DOS Error: '+message);loading.classList.remove('active');runBtn.disabled=false;runBtn.classList.remove('loading');updateLoadingProgress(0);document.dispatchEvent(new CustomEvent('compiler-run-end'));if(currentTcZipObjectUrl){URL.revokeObjectURL(currentTcZipObjectUrl);currentTcZipObjectUrl=null;}}else if(data.type==='SCREENSHOT_DATA'){if(data.error)return;let downloadUrl=null;if(data.blob instanceof Blob){downloadUrl=URL.createObjectURL(data.blob);}else if(data.dataUrl){downloadUrl=data.dataUrl;}
if(!downloadUrl)return;const link=document.createElement('a');link.href=downloadUrl;const now=new Date();const yyyy=now.getFullYear();const mm=String(now.getMonth()+1).padStart(2,'0');const dd=String(now.getDate()).padStart(2,'0');const hh=String(now.getHours()).padStart(2,'0');const min=String(now.getMinutes()).padStart(2,'0');link.download=`graphics_h_${yyyy}_${mm}_${dd}_${hh}_${min}.png`;document.body.appendChild(link);link.click();document.body.removeChild(link);if(data.blob instanceof Blob){setTimeout(()=>URL.revokeObjectURL(downloadUrl),1000);}}});async function runProgram(){if(!editor){alert('Editor is still loading. Please wait...');return;}
const code=editor.getValue();if(!code.trim()){alert('Please write some code first!');return;}
metrics.runtime.runCount++;Logger.info(`[Run] Triggered | count=${metrics.runtime.runCount}`);document.dispatchEvent(new CustomEvent('compiler-run-start'));const shouldPersistPreviewOnRun=window.__aiPreviewPending!==true&&window.__suppressCompileRunSave!==true;if(isUserLoggedIn&&shouldPersistPreviewOnRun){if(CLOUD_STATE.autosaveTimer){clearTimeout(CLOUD_STATE.autosaveTimer);CLOUD_STATE.autosaveTimer=null;}
const activeKey=CLOUD_STATE.activeFileKey||'root/main.cpp';const[folder,filename]=activeKey.split('/');setLocalDraftImmediate(folder,filename,code);localStorage.setItem('tc_code',code);forceSaveActiveFile('compileRun').catch(e=>{Logger.warn('Background save during run failed: '+e.message);});}else if(!isUserLoggedIn&&shouldPersistPreviewOnRun){saveCode();}
Logger.info('Starting compilation...');loading.classList.add('active');loadingText.textContent='Initializing DOS environment...';updateLoadingProgress(0);runBtn.disabled=true;runBtn.classList.add('loading');if(isMobileView()){switchMobileTab('output');}
if(isMobileView()&&isEditorFullscreen){toggleEditorFullscreen(false);}
outputPanel.classList.remove('visible');outputPanel.classList.remove('expanded');expandOutputBtn.classList.remove('expanded');expandOutputBtn.title='Expand panel';isOutputExpanded=false;terminalWrapper.classList.remove('has-panel');if(downloadTerminalBtn){downloadTerminalBtn.classList.add('hidden');}
lastErrorContent='';outputContent.textContent='';try{const iframe=await ensureDosRunnerFrame();if(!iframe.contentWindow){throw new Error('DOS terminal is not available.');}
updateLoadingProgress(20);await startPreload();const wdosboxUrl='/libs/wdosbox.js';Logger.info(`Using local WDOSBOX runtime: ${wdosboxUrl}`);updateLoadingProgress(40);const batchScript=`@ECHO OFF
CD TURBOC3\\BIN
IF EXIST USER.EXE DEL USER.EXE
IF EXIST ERR.TXT DEL ERR.TXT
IF EXIST FAIL.TXT DEL FAIL.TXT
TCC -I..\\INCLUDE -L..\\LIB -n. USER.CPP ..\\LIB\\GRAPHICS.LIB > ERR.TXT
IF EXIST USER.EXE GOTO SUCCESS
ECHO COMPILE_FAILED > FAIL.TXT
COPY ERR.TXT C:\\ERR.TXT >NUL
COPY FAIL.TXT C:\\FAIL.TXT >NUL
CLS
ECHO ========================================
ECHO COMPILATION ERRORS:
ECHO ========================================
TYPE ERR.TXT
ECHO.
PAUSE
EXIT
:SUCCESS
CLS
USER.EXE
PAUSE
`;loadingText.textContent='Loading compiler...';const tcBlob=await getTCZip();if(currentTcZipObjectUrl){URL.revokeObjectURL(currentTcZipObjectUrl);currentTcZipObjectUrl=null;}
currentTcZipObjectUrl=URL.createObjectURL(tcBlob);iframe.contentWindow.postMessage({type:'STOP_DOS'},'*');iframe.contentWindow.postMessage({type:'INIT_DOS',payload:{wdosboxUrl:wdosboxUrl,zipUrl:currentTcZipObjectUrl,code:code,batchScript:batchScript,cycles:isMobile?'auto':'max'}},'*');}catch(error){Logger.error('Failed to start DOS environment',error);if(error.message&&(error.message.includes('Compiler URL')||error.message.includes('Failed to download compiler')||error.message.includes('fetch')||error.message.includes('Failed to fetch'))){loading.innerHTML=`<div style="color: #ffb454; text-align: center; padding: 2rem; background: #2a2a2a; border-radius: 8px; border: 1px solid #ffb454; margin: auto;">
                <p style="margin-bottom: 1rem; font-size: 1.1rem;">⚠ Failed to load compiler files. Please refresh the page to try again.</p>
                <button onclick="window.location.reload()" style="padding: 0.5rem 1rem; cursor: pointer; background: #ffb454; color: #000; border: none; border-radius: 4px; font-weight: bold;">Refresh</button>
            </div>`;return;}
alert('Failed to start DOS environment. Error: '+error.message);loading.classList.remove('active');runBtn.disabled=false;runBtn.classList.remove('loading');updateLoadingProgress(0);if(currentTcZipObjectUrl){URL.revokeObjectURL(currentTcZipObjectUrl);currentTcZipObjectUrl=null;}}}
window.addEventListener('keydown',(e)=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter'&&!terminalFocused){e.preventDefault();runProgram();}
if((e.ctrlKey||e.metaKey)&&e.key==='s'&&!terminalFocused){e.preventDefault();saveCode();}});document.addEventListener('visibilitychange',()=>{if(document.hidden&&!isUserLoggedIn&&editor){localStorage.setItem('tc_code',editor.getValue());}});window.addEventListener('beforeunload',()=>{if(!editor)return;if(!isUserLoggedIn){try{localStorage.setItem('tc_code',editor.getValue());}catch(e){}}});async function prefetchDemoFiles(){try{const bundle=await loadDemoBundle();Object.entries(bundle).forEach(([key,code])=>{if(!DemoCache.get(key)){DemoCache.set(key,code);}});Logger.info('Pre-cached demo bundle');}catch(e){}}
(async function init(){Logger.info('Initializing compiler...');updateSaveIndicator();const loaded=await loadAllScripts();if(loaded){prefetchDemoFiles();Logger.success('Compiler ready');}})();;(function(){'use strict';const SETTINGS_DEFAULTS=(typeof APP_SETTINGS_DEFAULTS!=='undefined')?{editor:{...APP_SETTINGS_DEFAULTS.editor}}:{editor:{fontSize:16,wordWrap:true,lineNumbers:true,bracketMatching:true,activeLine:true,autocomplete:true,hoverTooltips:true,floatingRunBtn:true}};function cloneSettings(settings=SETTINGS_DEFAULTS){return{editor:{...settings.editor}};}
function loadSettingsState(){if(typeof loadAppSettings==='function'){return loadAppSettings();}
return cloneSettings();}
function saveSettingsState(settings){if(typeof saveAppSettings==='function'){return saveAppSettings(settings);}
return cloneSettings(settings);}
function emitSettingsChanged(){document.dispatchEvent(new CustomEvent('editor-settings-changed',{detail:{settings:cloneSettings(currentSettings)}}));}
function clampFontSize(size){return Math.max(10,Math.min(32,Number.parseInt(size,10)||SETTINGS_DEFAULTS.editor.fontSize));}
let currentSettings=loadSettingsState();let currentSidebarView='explorer';const settingsHeaderBtn=document.getElementById('settings-header-btn');const settingsActivityBtn=document.getElementById('settings-activity-btn');const mobileMenuSettingsBtn=document.getElementById('mobile-menu-settings-btn');const mobileMenuFilesBtn=document.getElementById('mobile-menu-files-btn');const explorerActivityBtn=document.getElementById('explorer-activity-btn');const settingsPanel=document.getElementById('settings-panel-view');const cloudPromoView=document.getElementById('cloud-promo-view');const fileExplorerView=document.getElementById('file-explorer-view');const sidebar=document.getElementById('sidebar');const sidebarHeader=document.querySelector('.sidebar-header');const themeToggleBtn=document.getElementById('theme-toggle-btn');const themeIconSun=document.getElementById('theme-icon-sun');const themeIconMoon=document.getElementById('theme-icon-moon');const fontRange=document.getElementById('settings-font-range');const fontSizeValue=document.getElementById('settings-font-size-value');const fontDecrease=document.getElementById('settings-font-decrease');const fontIncrease=document.getElementById('settings-font-increase');const wordWrapToggle=document.getElementById('settings-word-wrap');const lineNumbersToggle=document.getElementById('settings-line-numbers');const bracketMatchingToggle=document.getElementById('settings-bracket-matching');const activeLineToggle=document.getElementById('settings-active-line');const autocompleteToggle=document.getElementById('settings-autocomplete');const hoverTooltipsToggle=document.getElementById('settings-hover-tooltips');const floatingRunBtnToggle=document.getElementById('settings-floating-run-btn');const resetBtn=document.getElementById('settings-reset-btn');const headerFontDisplay=document.getElementById('font-size-display');function setSidebarHeading(label){if(!sidebarHeader)return;const headerLeft=sidebarHeader.querySelector('.sidebar-header-left span');if(headerLeft)headerLeft.textContent=label;}
function openSidebarIfNeeded(forceMobileOpen=false){if(sidebar&&sidebar.classList.contains('collapsed')){sidebar.classList.remove('collapsed');}
if(forceMobileOpen&&window.innerWidth<=768&&sidebar){sidebar.classList.add('open');const overlay=document.getElementById('sidebar-overlay');if(overlay)overlay.classList.add('active');document.body.classList.add('sidebar-open-mobile');}}
function setVisible(element,visible){if(!element)return;element.classList.toggle('hidden',!visible);}
function setSidebarView(view,options={}){currentSidebarView=view;setVisible(settingsPanel,view==='settings');const shouldShowExplorerFiles=view==='explorer'&&typeof isUserLoggedIn!=='undefined'&&isUserLoggedIn;const shouldShowExplorerPromo=view==='explorer'&&!shouldShowExplorerFiles;setVisible(fileExplorerView,shouldShowExplorerFiles);setVisible(cloudPromoView,shouldShowExplorerPromo);if(explorerActivityBtn)explorerActivityBtn.classList.toggle('active',view==='explorer');if(settingsActivityBtn)settingsActivityBtn.classList.toggle('active',view==='settings');if(mobileMenuFilesBtn)mobileMenuFilesBtn.classList.toggle('active',view==='explorer');if(mobileMenuSettingsBtn)mobileMenuSettingsBtn.classList.toggle('active',view==='settings');const explorerActionsEl=document.querySelector('.explorer-actions');setVisible(explorerActionsEl,shouldShowExplorerFiles);if(view==='settings'){setSidebarHeading('Settings');syncUIFromSettings();}else{setSidebarHeading('Files Explorer');}
openSidebarIfNeeded(options.forceMobileOpen===true);document.body.dataset.sidebarView=view;}
function showSettingsPanel(){setSidebarView('settings',{forceMobileOpen:true});}
function showExplorerPanel(options={}){setSidebarView('explorer',options);}
function toggleSettingsPanel(){if(currentSidebarView==='settings'){showExplorerPanel();return;}
showSettingsPanel();}
window.getSidebarView=()=>currentSidebarView;window.setSidebarView=(view)=>setSidebarView(view);function persistSettings(){currentSettings=saveSettingsState(currentSettings);emitSettingsChanged();}
function applyFontSize(size,save=true){const normalized=clampFontSize(size);if(editor){editor.setFontSize(`${normalized}px`);}
if(fontSizeValue)fontSizeValue.textContent=String(normalized);if(fontRange)fontRange.value=String(normalized);if(headerFontDisplay)headerFontDisplay.textContent=String(normalized);currentSettings={...currentSettings,editor:{...currentSettings.editor,fontSize:normalized}};if(save)persistSettings();}
function applyWordWrap(enabled,save=true){if(!cmView||!wordWrapCompartment||!cmModules)return;const{EditorView}=cmModules.view;cmView.dispatch({effects:wordWrapCompartment.reconfigure(enabled?EditorView.lineWrapping:[])});currentSettings={...currentSettings,editor:{...currentSettings.editor,wordWrap:!!enabled}};if(save)persistSettings();}
function applyLineNumbers(enabled,save=true){if(!cmView||!lineNumbersCompartment||!cmModules)return;const{lineNumbers}=cmModules.view;cmView.dispatch({effects:lineNumbersCompartment.reconfigure(enabled?lineNumbers():[])});currentSettings={...currentSettings,editor:{...currentSettings.editor,lineNumbers:!!enabled}};if(save)persistSettings();}
function applyBracketMatching(enabled,save=true){if(!cmView||!bracketMatchCompartment||!cmModules)return;const{bracketMatching}=cmModules.language;cmView.dispatch({effects:bracketMatchCompartment.reconfigure(enabled?bracketMatching():[])});currentSettings={...currentSettings,editor:{...currentSettings.editor,bracketMatching:!!enabled}};if(save)persistSettings();}
function applyActiveLine(enabled,save=true){if(!cmView||!activeLineCompartment||!cmModules)return;const{highlightActiveLine,highlightActiveLineGutter}=cmModules.view;cmView.dispatch({effects:activeLineCompartment.reconfigure(enabled?[highlightActiveLine(),highlightActiveLineGutter()]:[])});currentSettings={...currentSettings,editor:{...currentSettings.editor,activeLine:!!enabled}};if(save)persistSettings();}
function applyAutocomplete(enabled,save=true){currentSettings={...currentSettings,editor:{...currentSettings.editor,autocomplete:!!enabled}};if(save)persistSettings();}
function applyHoverTooltips(enabled,save=true){currentSettings={...currentSettings,editor:{...currentSettings.editor,hoverTooltips:!!enabled}};if(save)persistSettings();}
function applyFloatingRunBtn(enabled,save=true){const isDesktop=window.innerWidth>768;const floatBtn=document.getElementById('floating-run-btn');const floatTooltip=document.getElementById('floating-run-btn-tooltip');if(floatBtn){if(enabled&&isDesktop){floatBtn.style.removeProperty('display');}else{floatBtn.style.setProperty('display','none','important');}}
if(floatTooltip&&!enabled){floatTooltip.style.setProperty('display','none','important');}else if(floatTooltip&&enabled){floatTooltip.style.removeProperty('display');}
currentSettings={...currentSettings,editor:{...currentSettings.editor,floatingRunBtn:!!enabled}};if(save)persistSettings();}
function syncUIFromSettings(){if(fontRange)fontRange.value=String(currentSettings.editor.fontSize);if(fontSizeValue)fontSizeValue.textContent=String(currentSettings.editor.fontSize);if(wordWrapToggle)wordWrapToggle.checked=currentSettings.editor.wordWrap;if(lineNumbersToggle)lineNumbersToggle.checked=currentSettings.editor.lineNumbers;if(bracketMatchingToggle)bracketMatchingToggle.checked=currentSettings.editor.bracketMatching;if(activeLineToggle)activeLineToggle.checked=currentSettings.editor.activeLine;if(autocompleteToggle)autocompleteToggle.checked=currentSettings.editor.autocomplete!==false;if(hoverTooltipsToggle)hoverTooltipsToggle.checked=currentSettings.editor.hoverTooltips!==false;if(floatingRunBtnToggle)floatingRunBtnToggle.checked=currentSettings.editor.floatingRunBtn!==false;if(headerFontDisplay)headerFontDisplay.textContent=String(currentSettings.editor.fontSize);}
async function applySavedSettings(){if(!cmView||!cmModules)return;currentSettings=loadSettingsState();syncUIFromSettings();applyFontSize(currentSettings.editor.fontSize,false);applyWordWrap(currentSettings.editor.wordWrap,false);applyLineNumbers(currentSettings.editor.lineNumbers,false);applyBracketMatching(currentSettings.editor.bracketMatching,false);applyActiveLine(currentSettings.editor.activeLine,false);applyAutocomplete(currentSettings.editor.autocomplete!==false,false);applyHoverTooltips(currentSettings.editor.hoverTooltips!==false,false);applyFloatingRunBtn(currentSettings.editor.floatingRunBtn!==false,false);currentSettings=saveSettingsState(currentSettings);emitSettingsChanged();}
if(settingsActivityBtn){settingsActivityBtn.addEventListener('click',()=>{if(currentSidebarView==='settings'){if(sidebar)sidebar.classList.toggle('collapsed');return;}
showSettingsPanel();});}
if(mobileMenuSettingsBtn){mobileMenuSettingsBtn.addEventListener('click',showSettingsPanel);}
if(mobileMenuFilesBtn){mobileMenuFilesBtn.addEventListener('click',showExplorerPanel);}
if(explorerActivityBtn){explorerActivityBtn.addEventListener('click',()=>{if(currentSidebarView!=='explorer')showExplorerPanel();});}
document.addEventListener('request-show-explorer',()=>{if(currentSidebarView!=='explorer')showExplorerPanel();});if(settingsHeaderBtn){settingsHeaderBtn.addEventListener('click',toggleSettingsPanel);}
document.addEventListener('request-show-settings',()=>{if(currentSidebarView!=='settings')showSettingsPanel();});document.addEventListener('auth-state-changed',()=>{if(currentSidebarView==='explorer'){showExplorerPanel();}});if(fontRange){fontRange.addEventListener('input',(event)=>{applyFontSize(event.target.value,true);});}
if(fontDecrease){fontDecrease.addEventListener('click',()=>{applyFontSize(currentSettings.editor.fontSize-1,true);});}
if(fontIncrease){fontIncrease.addEventListener('click',()=>{applyFontSize(currentSettings.editor.fontSize+1,true);});}
if(wordWrapToggle){wordWrapToggle.addEventListener('change',(event)=>{applyWordWrap(event.target.checked,true);});}
if(lineNumbersToggle){lineNumbersToggle.addEventListener('change',(event)=>{applyLineNumbers(event.target.checked,true);});}
if(bracketMatchingToggle){bracketMatchingToggle.addEventListener('change',(event)=>{applyBracketMatching(event.target.checked,true);});}
if(activeLineToggle){activeLineToggle.addEventListener('change',(event)=>{applyActiveLine(event.target.checked,true);});}
if(autocompleteToggle){autocompleteToggle.addEventListener('change',(event)=>{applyAutocomplete(event.target.checked,true);});}
if(hoverTooltipsToggle){hoverTooltipsToggle.addEventListener('change',(event)=>{applyHoverTooltips(event.target.checked,true);});}
if(floatingRunBtnToggle){floatingRunBtnToggle.addEventListener('change',(event)=>{applyFloatingRunBtn(event.target.checked,true);});}
if(resetBtn){resetBtn.addEventListener('click',()=>{currentSettings=cloneSettings(SETTINGS_DEFAULTS);applyFontSize(currentSettings.editor.fontSize,false);applyWordWrap(currentSettings.editor.wordWrap,false);applyLineNumbers(currentSettings.editor.lineNumbers,false);applyBracketMatching(currentSettings.editor.bracketMatching,false);applyActiveLine(currentSettings.editor.activeLine,false);applyAutocomplete(currentSettings.editor.autocomplete!==false,false);applyHoverTooltips(currentSettings.editor.hoverTooltips!==false,false);applyFloatingRunBtn(currentSettings.editor.floatingRunBtn!==false,false);syncUIFromSettings();persistSettings();Logger.info('Settings reset to defaults');});}
function applyTheme(theme,save=true){if(theme==='light'){document.documentElement.setAttribute('data-theme','light');if(themeIconSun)themeIconSun.style.display='none';if(themeIconMoon)themeIconMoon.style.display='block';}else{document.documentElement.removeAttribute('data-theme');if(themeIconSun)themeIconSun.style.display='block';if(themeIconMoon)themeIconMoon.style.display='none';}
if(save){localStorage.setItem('app-theme',theme);}}
if(themeToggleBtn){themeToggleBtn.addEventListener('click',()=>{const isLight=document.documentElement.getAttribute('data-theme')==='light';applyTheme(isLight?'dark':'light');});}
const savedTheme=localStorage.getItem('app-theme')||'dark';applyTheme(savedTheme,false);document.addEventListener('editor-ready',()=>{applySavedSettings();});document.addEventListener('editor-font-size-change-requested',(event)=>{const requestedSize=event.detail?.fontSize;applyFontSize(requestedSize,true);});syncUIFromSettings();setSidebarView('explorer',{forceMobileOpen:false});applyFloatingRunBtn(currentSettings.editor.floatingRunBtn!==false,false);if(typeof cmView!=='undefined'&&cmView&&cmModules){applySavedSettings();}})();;