{
  "name": "r2-d1-migration",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "migrate": "node migrate.js",
    "reset:remote": "wrangler d1 execute graphicsh_oc_db --remote --file=./reset.sql --config=./wrangler.jsonc",
    "import:remote": "wrangler d1 execute graphicsh_oc_db --remote --file=./migration.sql --config=./wrangler.jsonc"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "csv-parse": "^5.0.0",
    "dotenv": "^16.0.0"
  }
}
